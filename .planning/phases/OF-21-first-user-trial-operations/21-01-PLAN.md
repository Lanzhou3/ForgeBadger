# First-User Trial Operations Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Select v1.5 and define the first-user trial operations loop that routes real evidence, feedback, and remaining caveats without expanding runtime authority.

**Architecture:** This is a planning and evidence-operations phase. The active source of truth moves from v1.4 closeout to v1.5 trial operations, while the external evidence registry remains authoritative for gate states. No Gateway/Web runtime behavior changes in this plan.

**Tech Stack:** Markdown, GSD planning files, `rg`, `git diff --check`.

---

### Task 1: Select v1.5 And Preserve Gate Truth

**Files:**
- Modify: `.planning/ROADMAP.md`
- Modify: `.planning/REQUIREMENTS.md`
- Modify: `.planning/STATE.md`
- Modify: `.planning/PROJECT.md`
- Modify: `.planning/MILESTONES.md`
- Modify: `.planning/DECISIONS-INDEX.md`
- Modify: `MEMORY.md`
- Read: `docs/EXTERNAL-EVIDENCE-GATES.md`
- Read: `docs/reports/v1.4-external-evidence-closeout-2026-05-29.md`

- [x] **Step 1: Confirm current source-of-truth state**

Run:

```bash
git status --short --branch
sed -n '1,220p' .planning/STATE.md
sed -n '1,260p' .planning/ROADMAP.md
sed -n '1,220p' docs/EXTERNAL-EVIDENCE-GATES.md
```

Expected before Phase 21 edits: v1.4 complete, milestone selection pending,
and external gates still `Caveat` or `Blocked`.

- [x] **Step 2: Select v1.5**

Update roadmap, state, project, milestones, decisions, and memory so the next
milestone is:

```text
v1.5 First-User Trial Operations
```

The milestone goal is to operationalize first-user trial evidence and feedback,
not to add remote execution or autonomous features.

- [x] **Step 3: Preserve v1.4 gate states**

Keep:

- `LIVE-PROVIDER`: `Caveat`
- `WINDOWS-WSL`: `Caveat`
- `FEISHU-CALLBACK`: `Blocked`
- `FIRST-USER-FEEDBACK`: `Caveat`

Do not move any gate to `Pass` without the required artifact.

### Task 2: Define Phase 21 Trial-Operations Contract

**Files:**
- Create: `.planning/phases/OF-21-first-user-trial-operations/21-CONTEXT.md`
- Create: `.planning/phases/OF-21-first-user-trial-operations/21-01-PLAN.md`
- Create: `docs/superpowers/specs/2026-05-29-openforge-v1.5-first-user-trial-operations-design.md`
- Modify: `docs/TRIAL-FEEDBACK.md`
- Modify: `.github/ISSUE_TEMPLATE/openforge-trial-feedback.yml`
- Modify: `.gitignore`

- [x] **Step 1: Capture Phase 21 context**

Write why Phase 21 exists, which artifacts it depends on, what it can accept,
and what it must reject.

- [x] **Step 2: Define the trial packet**

The active requirements should require a completed first-user trial packet to
include:

- affected surface;
- severity;
- owner;
- disposition or next action;
- environment summary;
- reproduction steps;
- diagnostics status or reason diagnostics are unavailable;
- follow-up issue, phase, or no-action rationale;
- explicit redaction review.

- [x] **Step 3: Update active intake artifacts**

Update `docs/TRIAL-FEEDBACK.md` and
`.github/ISSUE_TEMPLATE/openforge-trial-feedback.yml` so real user submissions
capture the Phase 21 packet fields. Remove first-user instructions that require
retrieving browser auth tokens from developer tools. Replace open-ended log and
command-output requests with bounded redacted summaries.

- [x] **Step 4: Define routing**

Map trial outcomes to:

- `docs/EXTERNAL-EVIDENCE-GATES.md`;
- `docs/TRIAL-FEEDBACK.md`;
- `.github/ISSUE_TEMPLATE/openforge-trial-feedback.yml`;
- follow-up issues #3, #4, #5 where relevant;
- a future report only when a real artifact exists.

### Task 3: Verify The New Milestone State

**Files:**
- All files touched in Tasks 1 and 2

- [x] **Step 1: Verify milestone references**

Run:

```bash
rg -n "v1.5 First-User Trial Operations|Phase 21|First-User Trial Operations|TRIALOPS|LIVE-PROVIDER|WINDOWS-WSL|FEISHU-CALLBACK|FIRST-USER-FEEDBACK" \
  .planning/STATE.md \
  .planning/ROADMAP.md \
  .planning/REQUIREMENTS.md \
  .planning/PROJECT.md \
  .planning/MILESTONES.md \
  .planning/DECISIONS-INDEX.md \
  .planning/phases/OF-21-first-user-trial-operations \
  MEMORY.md \
  docs/superpowers/specs/2026-05-29-openforge-v1.5-first-user-trial-operations-design.md
```

Expected: current planning files point at v1.5/Phase 21, while external gate
states remain visible.

- [x] **Step 2: Verify canonical gate states**

Run:

```bash
rg -n '^\\| `LIVE-PROVIDER` \\| `Caveat` \\|' docs/EXTERNAL-EVIDENCE-GATES.md
rg -n '^\\| `WINDOWS-WSL` \\| `Caveat` \\|' docs/EXTERNAL-EVIDENCE-GATES.md
rg -n '^\\| `FEISHU-CALLBACK` \\| `Blocked` \\|' docs/EXTERNAL-EVIDENCE-GATES.md
rg -n '^\\| `FIRST-USER-FEEDBACK` \\| `Caveat` \\|' docs/EXTERNAL-EVIDENCE-GATES.md
rg -n '^\\| `LIVE-PROVIDER` \\| Caveat \\|' docs/reports/v1.4-external-evidence-closeout-2026-05-29.md
rg -n '^\\| `WINDOWS-WSL` \\| Caveat \\|' docs/reports/v1.4-external-evidence-closeout-2026-05-29.md
rg -n '^\\| `FEISHU-CALLBACK` \\| Blocked \\|' docs/reports/v1.4-external-evidence-closeout-2026-05-29.md
rg -n '^\\| `FIRST-USER-FEEDBACK` \\| Caveat \\|' docs/reports/v1.4-external-evidence-closeout-2026-05-29.md
```

Expected: all four canonical registry states and v1.4 closeout states remain
unchanged.

- [x] **Step 3: Verify no unsafe evidence request was introduced**

Run:

```bash
rg --glob '!21-01-PLAN.md' --glob '!21-01-SUMMARY.md' -n "get .*token|developer tools.*token|paste the .*token|paste your .*key|Please attach raw|Please upload raw|Please submit raw|Attach raw|Upload raw|Submit raw|raw terminal transcript request|raw provider payload request|raw Feishu body request" docs/TRIAL-FEEDBACK.md .github/ISSUE_TEMPLATE/openforge-trial-feedback.yml .planning/phases/OF-21-first-user-trial-operations docs/superpowers/specs/2026-05-29-openforge-v1.5-first-user-trial-operations-design.md || true
```

Expected: no instruction asks users to submit secrets or raw private evidence.
Mentions in non-goals, redaction warnings, and verification commands are
acceptable only when phrased as prohibitions.

- [x] **Step 4: Verify Markdown and worktree state**

Run:

```bash
git diff --check
git status --short --branch
```

Expected: no whitespace errors; only intended planning/docs files are modified,
plus unrelated `upload_img/` remains untracked.
