# Trial Materials Consistency Guard Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use test-driven-development for
> validator behavior and verification-before-completion before claiming
> closure.

**Goal:** Extend the trial feedback intake validator so the trial checklist
cannot drift away from the runbook, feedback template, issue form, packet audit,
and external gate validator.

---

### Task 1: Define Checklist Drift Coverage

**Files:**
- Modify: `scripts/validate-trial-feedback-intake.test.mjs`

- [x] **Step 1: Write failing test**

The test proves checklist content fails when it removes:

- `pnpm trial:intake-validate`
- `pnpm trial:feedback-audit`
- `pnpm evidence:gates-validate`
- browser-token safety boundaries

- [x] **Step 2: Verify red**

Run:

```bash
node scripts/validate-trial-feedback-intake.test.mjs
```

Expected red result before implementation:

- the new checklist drift test returns `ok: true` because checklist content is
  ignored.

### Task 2: Implement Checklist Validation

**Files:**
- Modify: `scripts/validate-trial-feedback-intake.mjs`
- Modify: `package.json`

- [x] **Step 1: Read checklist by default**

Add `docs/TRIAL-CHECKLIST.md` to the existing validator inputs.

- [x] **Step 2: Enforce required checklist anchors**

Require the checklist to preserve first-user entry-point wording, gate registry
reference, intake validator command, packet audit command, gate validator
command, `FIRST-USER-FEEDBACK` boundary, follow-up routing, and redaction
review.

- [x] **Step 3: Reuse safety checks**

Reject unsafe raw-evidence wording and non-negated browser-token guidance in
the checklist.

### Task 3: Update Trial Docs

**Files:**
- Modify: `docs/TRIAL-RUNBOOK.md`
- Modify: `docs/TRIAL-CHECKLIST.md`
- Modify: `docs/TRIAL-FEEDBACK.md`
- Modify: `docs/CI-CD-PLAN.md`
- Modify: `.github/workflows/ci.yml`

- [x] **Step 1: Document local validator**

Document `pnpm trial:intake-validate` as a materials-consistency guard before
editing trial intake docs.

- [x] **Step 2: Preserve evidence boundary**

State that validation does not collect first-user evidence, submit feedback, or
clear external gates.

- [x] **Step 3: Add CI command coverage**

Run both root validators after script harness tests in CI:

```bash
pnpm trial:intake-validate
pnpm evidence:gates-validate
```

### Task 4: Verify

- [x] **Step 1: Run focused test and CLI smoke**

```bash
node scripts/validate-trial-feedback-intake.test.mjs
pnpm trial:intake-validate
```

- [x] **Step 2: Run script harness and gate validator**

```bash
node --test scripts/validate-external-evidence-gates.test.mjs scripts/audit-trial-feedback-packet.test.mjs scripts/create-trial-feedback-draft.test.mjs scripts/validate-trial-feedback-intake.test.mjs scripts/run-with-root-env.test.mjs scripts/smoke-codex-app-server.test.mjs scripts/smoke-local-release.test.mjs
pnpm evidence:gates-validate
```

- [x] **Step 3: Verify references and gate states**

```bash
rg -n 'trial:intake-validate|REQUIRED_CHECKLIST_PHRASES|pnpm trial:feedback-audit|pnpm evidence:gates-validate|FIRST-USER-FEEDBACK' package.json docs scripts .planning MEMORY.md
rg -n '\| `LIVE-PROVIDER` \| `Caveat`|\| `WINDOWS-WSL` \| `Caveat`|\| `FEISHU-CALLBACK` \| `Blocked`|\| `FIRST-USER-FEEDBACK` \| `Caveat`' docs/EXTERNAL-EVIDENCE-GATES.md
git diff --check
```
