---
phase: 10-goal-and-work-item-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/projects/ProjectManagerPanel.tsx
  - packages/web/src/components/ui/textarea.tsx
  - packages/web/src/lib/i18n.ts
  - packages/web/e2e/project-manager.spec.ts
autonomous: true
requirements: [PMUX-02]
requirements_addressed: [PMUX-02]
user_setup: []
must_haves:
  truths:
    - "D-01: Goal editing stays inside the existing Project Manager tab."
    - "D-02: Goal edit/create uses `PUT /goal` with summary, constraints, acceptance criteria, and status."
    - "D-03: Constraints and acceptance criteria use newline-separated textareas normalized into arrays."
    - "D-04: Goal status defaults to `active`; Gateway remains authoritative for errors."
    - "D-14: Goal mutations refetch relevant Project Manager data after success."
    - "D-15: Focused Web tests cover goal save success/error behavior."
    - "D-16: Strict Playwright coverage keeps unknown `/api/v1/*` routes failing fast while proving goal update."
  artifacts:
    - "packages/web/src/components/projects/ProjectManagerPanel.tsx"
    - "packages/web/src/components/ui/textarea.tsx"
    - "packages/web/src/lib/i18n.ts"
    - "packages/web/e2e/project-manager.spec.ts"
  key_links:
    - "`10-UI-SPEC.md` defines goal edit copy, layout, spacing, and accessibility."
    - "`docs/API.md` defines the Gateway-owned goal endpoint contract."
---

<objective>
Implement inline project goal editing in the Project Manager tab.

Purpose: PMUX-02 requires users to view and update goal summary, constraints,
acceptance criteria, and status without leaving the project detail workflow.
Output: The existing Project Manager goal card supports create/edit/save/cancel,
normalizes text lists into arrays, shows mutation errors, and refreshes
persisted Gateway data after save.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-CONTEXT.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-RESEARCH.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-PATTERNS.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-VALIDATION.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md
@docs/API.md
@packages/web/src/components/projects/ProjectManagerPanel.tsx
@packages/web/src/lib/api.ts
@packages/web/src/lib/i18n.ts
@packages/web/e2e/project-manager.spec.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Integrity | Goal edit form | high | mitigate | Normalize newline text into arrays locally, but submit only Gateway-supported fields and display server validation errors without inventing Web authority. |
| T-10-02 | Information disclosure | Goal mutation errors | medium | mitigate | Render bounded error messages near the goal form and do not log or display raw request payloads beyond user-entered form text. |
| T-10-03 | Availability | Project Manager tab state | medium | mitigate | Preserve `enabled`/`canLoad` guards and keep existing read data visible when a goal mutation fails. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add reusable textarea styling for Project Manager forms</name>
  <files>packages/web/src/components/ui/textarea.tsx</files>
  <read_first>packages/web/src/components/ui/textarea.tsx (create if missing), packages/web/src/components/ui/input.tsx, packages/web/src/components/ui/form.tsx, .planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md</read_first>
  <action>Create `packages/web/src/components/ui/textarea.tsx` exporting `Textarea`. Match the existing `Input` token pattern: full width, rounded-md, border-input, transparent/background input styling, placeholder muted foreground, disabled opacity, focus-visible border/ring, aria-invalid destructive border/ring, and `md:text-sm`. Use a native `textarea`, set a reasonable minimum height such as `min-h-24`, and do not fetch registry components.</action>
  <verify>
    <automated>rg -n "function Textarea|export \\{ Textarea \\}|aria-invalid|focus-visible:ring" packages/web/src/components/ui/textarea.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/web/src/components/ui/textarea.tsx` exists.
    - The component exports `Textarea`.
    - The component uses semantic tokens matching `Input`.
    - The component includes `aria-invalid` styling.
    - No external registry file or dependency is added.
  </acceptance_criteria>
  <done>A local textarea primitive exists for goal and work item forms.</done>
</task>

<task type="auto">
  <name>Task 2: Implement inline goal edit/create flow</name>
  <files>packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/components/ui/textarea.tsx, packages/web/src/lib/api.ts, docs/API.md, .planning/phases/OF-10-goal-and-work-item-operations/10-CONTEXT.md, .planning/phases/OF-10-goal-and-work-item-operations/10-RESEARCH.md, .planning/phases/OF-10-goal-and-work-item-operations/10-PATTERNS.md, .planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md</read_first>
  <action>Extend `ProjectManagerPanel` goal handling. Import `useMutation`, `useQueryClient`, `updateProjectManagerGoal`, `Input`, `Label`, and `Textarea`. Add local helpers `parseProjectManagerTextList(value: string): string[]` and `formatProjectManagerTextList(values: string[]): string` that split by newline, trim entries, and drop empty lines. Add goal edit state for summary, constraintsText, acceptanceCriteriaText, and status. Empty goal state opens the same form with summary empty, lists empty, and status `active`. Existing goal edit form is prefilled from current goal. Save calls `updateProjectManagerGoal(projectId, { summary, constraints, acceptanceCriteria, status })`, disables controls while pending, renders a goal-specific mutation error in the goal card, exits edit mode on success, and refetches/invalidate the `project-manager` goal plus visible ledger data. Cancel restores read state and does not mutate.</action>
  <verify>
    <automated>rg -n "updateProjectManagerGoal|parseProjectManagerTextList|formatProjectManagerTextList|projectManagerEditGoal|projectManagerSaveGoal|projectManagerGoalSummary|projectManagerGoalMutationError" packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Empty goal state exposes a goal create/edit action.
    - Existing goal state exposes an edit action.
    - Goal form includes summary, constraints, acceptance criteria, and status fields.
    - Constraint and acceptance criteria textareas normalize to arrays by newline trim/drop-empty.
    - Save calls `updateProjectManagerGoal` and does not call any Next.js API route.
    - Mutation errors remain visible in the goal card.
    - Existing read data is not cleared on mutation failure.
    - Successful save refreshes visible Project Manager data.
  </acceptance_criteria>
  <done>The Project Manager goal card supports safe inline editing and persisted refresh.</done>
</task>

<task type="auto">
  <name>Task 3: Add localized goal edit copy</name>
  <files>packages/web/src/lib/i18n.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/lib/i18n.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx, .planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md</read_first>
  <action>Add Phase 10 goal edit strings to every existing dictionary in `packages/web/src/lib/i18n.ts`. Required keys: `projects.projectManagerEditGoal`, `projects.projectManagerSaveGoal`, `projects.projectManagerCancel`, `projects.projectManagerGoalSummary`, `projects.projectManagerGoalStatus`, `projects.projectManagerGoalMutationError`, `projects.projectManagerGoalSummaryRequired`, `projects.projectManagerTextListHint`, and any additional goal form helper keys used by `ProjectManagerPanel`. Wire all new visible goal edit labels/errors through `t(...)` instead of inline strings.</action>
  <verify>
    <automated>rg -n "projectManagerEditGoal|projectManagerSaveGoal|projectManagerGoalSummary|projectManagerGoalMutationError|projectManagerTextListHint" packages/web/src/lib/i18n.ts packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Goal edit keys exist in Simplified Chinese, Traditional Chinese, and English dictionaries.
    - `ProjectManagerPanel` uses translation keys for every visible goal edit label, helper, action, and error.
    - No visible GSD or architecture explanation text is added to the UI.
  </acceptance_criteria>
  <done>Goal edit copy is localized through the existing i18n surface.</done>
</task>

<task type="auto">
  <name>Task 4: Cover goal edit in strict Project Manager E2E</name>
  <files>packages/web/e2e/project-manager.spec.ts</files>
  <read_first>packages/web/e2e/project-manager.spec.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/lib/api.ts, .planning/phases/OF-10-goal-and-work-item-operations/10-VALIDATION.md, .planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md</read_first>
  <action>Extend `packages/web/e2e/project-manager.spec.ts` to cover goal update. In the existing strict mock, handle `PUT /api/v1/projects/${PROJECT_ID}/project-manager/goal`; assert the request body includes `summary`, `constraints`, `acceptanceCriteria`, and `status`. Fulfill with an updated goal. Add a test that opens the Project Manager tab, edits the goal, enters newline constraints/criteria, saves, sees the updated summary, and asserts no unhandled API routes were recorded. Preserve the 404 fallback for unknown `/api/v1/*` routes.</action>
  <verify>
    <automated>pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - E2E covers opening the Project Manager tab and saving a goal update.
    - The mock asserts the `PUT /goal` body contains normalized arrays.
    - The UI shows the persisted updated goal after save.
    - Unknown `/api/v1/*` routes still return 404 and are asserted as absent.
  </acceptance_criteria>
  <done>Goal edit workflow has strict E2E route-contract coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/web run typecheck`
- [ ] `pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium`
- [ ] `git diff --check`
- [ ] `rg -n "updateProjectManagerGoal|projectManagerEditGoal|Textarea|projectManagerGoalMutationError" packages/web/src/components/projects/ProjectManagerPanel.tsx packages/web/src/components/ui/textarea.tsx packages/web/src/lib/i18n.ts packages/web/e2e/project-manager.spec.ts`
</verification>

<success_criteria>
- PMUX-02 is covered for goal view/update from the project context.
- Goal form values persist through Gateway and refresh visible read state.
- Constraint and acceptance criteria textareas submit arrays, not raw JSON.
- Mutation errors remain visible and do not blank the goal card.
- The goal edit flow follows `10-UI-SPEC.md` and does not add backend or Next.js API authority.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-10-goal-and-work-item-operations/10-01-SUMMARY.md`.
</output>
