# Trial Issue Route Preflight Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use test-driven-development for
> validator behavior and verification-before-completion before claiming
> closure.

**Goal:** Add a maintainer-run preflight that verifies GitHub issue routes #3,
#4, and #5 remain usable before routing real first-user trial evidence.

---

### Task 1: Define Issue Route Contract

**Files:**
- Create: `scripts/validate-trial-issue-routes.test.mjs`

- [x] **Step 1: Write failing tests**

Tests must prove:

- expected open issue routes pass;
- missing issues fail;
- closed issues fail;
- wrong titles fail;
- missing required labels fail;
- validation is not gate-clearing evidence.

- [x] **Step 2: Verify red**

Run:

```bash
node --test scripts/validate-trial-issue-routes.test.mjs
```

Expected red result before implementation:

- `ERR_MODULE_NOT_FOUND` for `scripts/validate-trial-issue-routes.mjs`.

### Task 2: Implement Validator

**Files:**
- Create: `scripts/validate-trial-issue-routes.mjs`
- Modify: `package.json`

- [x] **Step 1: Define expected routes**

Require:

- issue #3 -> `LIVE-PROVIDER`
- issue #4 -> `WINDOWS-WSL`
- issue #5 -> `FIRST-USER-FEEDBACK`

- [x] **Step 2: Validate metadata**

Require each issue to be readable, `OPEN`, exact-title matched, and to keep its
required labels.

- [x] **Step 3: Keep command read-only**

Use `gh issue view` only. Do not create, mutate, close, label, or comment on
issues.

### Task 3: Update Docs And CI

**Files:**
- Modify: `.github/workflows/ci.yml`
- Modify: `docs/CI-CD-PLAN.md`
- Modify: `docs/TRIAL-RUNBOOK.md`
- Modify: `docs/TRIAL-CHECKLIST.md`
- Modify: `docs/TRIAL-FEEDBACK.md`
- Modify: `scripts/validate-trial-feedback-intake.mjs`
- Modify: `scripts/validate-trial-feedback-intake.test.mjs`

- [x] **Step 1: Add script harness coverage**

Add `scripts/validate-trial-issue-routes.test.mjs` to the CI script harness.

- [x] **Step 2: Link preflight from trial docs**

Document `pnpm trial:issue-routes-validate` as a maintainer preflight before
routing real feedback to existing GitHub issues.

- [x] **Step 3: Keep intake materials guarded**

Add the preflight command to the checklist anchors validated by
`pnpm trial:intake-validate`.

### Task 4: Verify

- [x] **Step 1: Run focused tests**

```bash
node --test scripts/validate-trial-issue-routes.test.mjs
node --test scripts/validate-trial-feedback-intake.test.mjs
```

- [x] **Step 2: Run live maintainer preflight**

```bash
pnpm trial:issue-routes-validate
```

Expected:

- `ok: true`
- issues #3, #4, and #5 are `OPEN`
- `gateClearingEvidence: false`

- [x] **Step 3: Run script harness and existing validators**

```bash
node --test scripts/validate-external-evidence-gates.test.mjs scripts/validate-trial-issue-routes.test.mjs scripts/audit-trial-feedback-packet.test.mjs scripts/create-trial-feedback-draft.test.mjs scripts/validate-trial-feedback-intake.test.mjs scripts/run-with-root-env.test.mjs scripts/smoke-codex-app-server.test.mjs scripts/smoke-local-release.test.mjs
pnpm trial:intake-validate
pnpm evidence:gates-validate
```

- [x] **Step 4: Verify references and gate states**

```bash
rg -n 'trial:issue-routes-validate|validate-trial-issue-routes|gateClearingEvidence|issue #3|issue #4|issue #5' package.json docs scripts .github .planning MEMORY.md
rg -n '\| `LIVE-PROVIDER` \| `Caveat`|\| `WINDOWS-WSL` \| `Caveat`|\| `FEISHU-CALLBACK` \| `Blocked`|\| `FIRST-USER-FEEDBACK` \| `Caveat`' docs/EXTERNAL-EVIDENCE-GATES.md
git diff --check
```
