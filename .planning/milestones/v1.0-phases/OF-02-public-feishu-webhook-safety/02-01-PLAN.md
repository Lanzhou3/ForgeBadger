---
phase: 02-public-feishu-webhook-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/API.md
autonomous: true
requirements:
  - FSH-01
  - FSH-02
  - FSH-03
  - FSH-04
user_setup: []
must_haves:
  truths:
    - "D-01: `POST /api/v1/integrations/feishu/inbound` remains the JWT-protected test adapter."
    - "D-02: Public webhook docs require a per-integration public id."
    - "D-03: Public webhook docs require explicit disabled-by-default enablement."
    - "D-04: Public Feishu protocol responses are documented separately from the OpenForge REST envelope."
    - "D-05: Docs specify ordinary event signature headers and raw-body verification."
    - "D-06: Docs specify a narrow timestamp freshness window before event normalization."
    - "D-07: Docs specify setup-only URL verification challenge behavior with no Copilot side effects."
    - "D-08: Docs specify the encrypted-payload support boundary."
    - "D-09: Docs specify a persistent replay store separate from audit logs and in-memory maps."
    - "D-10: Docs state SQLite-backed protection is only for local single-Gateway deployment."
    - "D-11: Docs state multi-instance public webhook use requires a shared replay/rate-limit store before enablement."
    - "D-12: Docs specify per tenant/integration, per chat, and mapped-user rate-limit scopes."
    - "D-13: Docs preserve fail-closed tenant policy before any Copilot side effect."
    - "D-14: Docs limit initial public webhook support to minimum command-bridge message events."
    - "D-15: Docs state free-form approval text remains non-authoritative."
    - "D-16: Docs state Feishu remains Copilot ingress, not terminal or project-manager ledger."
    - "D-17: Docs prohibit sensitive request material in failure logs or responses."
    - "D-18: Docs specify redacted audit rows for policy rejections."
    - "D-19: Docs specify accepted-event audit metadata fields."
    - "D-20: Docs preserve provider/API redaction and no terminal input semantics."
  artifacts:
    - "docs/API.md"
  key_links:
    - "docs/API.md has a public webhook subsection under Feishu Integrations."
    - "docs/API.md explicitly separates public Feishu protocol responses from OpenForge authenticated REST envelopes."
---

<objective>
Specify the public Feishu webhook contract before implementation.

Purpose: planners and executors must have a concrete boundary for route shape, Feishu verification, replay/rate-limit topology, failure responses, audit, and non-goals before code exposes any public ingress.
Output: `docs/API.md` public webhook contract and test matrix language that covers FSH-01 through FSH-04.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/OF-02-public-feishu-webhook-safety/02-CONTEXT.md
@.planning/phases/OF-02-public-feishu-webhook-safety/02-RESEARCH.md
@.planning/phases/OF-02-public-feishu-webhook-safety/02-PATTERNS.md
@docs/API.md
@docs/superpowers/specs/2026-05-17-feishu-project-manager-copilot-design.md
@docs/superpowers/plans/2026-05-19-feishu-inbound-command-bridge-next.md
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Spoofing | Public Feishu webhook contract | high | mitigate | Document signature, timestamp, nonce, verification token, and challenge boundaries before implementation. |
| T-02-02 | Integrity | API docs and execution plan | high | mitigate | Keep public webhook route separate from JWT test adapter and explicitly state no free-form approval or terminal input. |
| T-02-03 | Information disclosure | Public failure responses and docs | medium | mitigate | Document minimal protocol responses and prohibit tokens, signatures, encrypt keys, raw text, and credentials in docs/audit/diagnostics. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add public webhook route contract to docs/API.md</name>
  <files>docs/API.md</files>
  <read_first>docs/API.md, .planning/phases/OF-02-public-feishu-webhook-safety/02-CONTEXT.md, .planning/phases/OF-02-public-feishu-webhook-safety/02-RESEARCH.md</read_first>
  <action>Add a Feishu public webhook subsection under `### Integrations`. It must name `POST /api/v1/integrations/feishu/webhook/:publicId`, state that `/inbound` remains JWT-protected test adapter, and document disabled-by-default enablement with per-integration public id, encrypted verification token, and encrypted event encrypt key. Include exact header names `X-Lark-Request-Timestamp`, `X-Lark-Request-Nonce`, and `X-Lark-Signature`. State that URL verification returns only `{\"challenge\":\"...\"}` and ordinary accepted/ignored events return minimal Feishu-compatible JSON, not the OpenForge REST envelope.</action>
  <verify>rg -n "webhook/:publicId|/inbound.*test adapter|X-Lark-Request-Timestamp|X-Lark-Request-Nonce|X-Lark-Signature|\\{\"challenge\"|OpenForge REST envelope" docs/API.md</verify>
  <acceptance_criteria>
    - `docs/API.md` contains `POST /api/v1/integrations/feishu/webhook/:publicId`.
    - `docs/API.md` says `/inbound` remains a JWT-protected test adapter.
    - `docs/API.md` contains all three Feishu header names.
    - `docs/API.md` contains `{"challenge"` in the public webhook section.
  </acceptance_criteria>
  <done>Public webhook route contract is documented before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Document replay, rate-limit, and deployment topology contract</name>
  <files>docs/API.md</files>
  <read_first>docs/API.md, .planning/phases/OF-02-public-feishu-webhook-safety/02-CONTEXT.md</read_first>
  <action>Extend the public webhook subsection with persistent replay and rate-limit semantics: replay keys include tenant/integration plus Feishu event id or message id; nonce/signature replay is rejected within the timestamp window; per tenant/integration, per chat, and per mapped OpenForge user rate limits apply. State that SQLite-backed storage is supported only for the local single-Gateway deployment and multi-instance public webhook use requires a shared replay/rate-limit store before enablement.</action>
  <verify>rg -n "replay key|event id|message id|nonce|per chat|mapped OpenForge user|single-Gateway|shared replay|shared rate" docs/API.md</verify>
  <acceptance_criteria>
    - `docs/API.md` contains `single-Gateway`.
    - `docs/API.md` contains `shared replay` or `shared rate`.
    - `docs/API.md` contains `per chat`.
    - `docs/API.md` contains `mapped OpenForge user`.
  </acceptance_criteria>
  <done>Replay and rate-limit deployment topology is explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Document fail-closed policy, audit, redaction, and non-goals</name>
  <files>docs/API.md</files>
  <read_first>docs/API.md, docs/superpowers/specs/2026-05-17-feishu-project-manager-copilot-design.md, docs/superpowers/plans/2026-05-19-feishu-inbound-command-bridge-next.md</read_first>
  <action>Extend `docs/API.md` to state that after signature/replay/rate checks, public events normalize into the existing bounded inbound command policy: enabled config, not emergency disabled, identity mode `user` or `bot`, explicit `allowedChatIds`, mapped Feishu user, visible `projectId`, active-run blocking, redaction, and audit. State that `approve`, `批准`, `/approve <id>`, and any direct terminal input remain unsupported. Add the audit fields: public id or integration id, event/message id, chat id, mapped OpenForge user id, optional project id, run id, conversation id, pending action count, and redacted text summary.</action>
  <verify>rg -n "emergency|identity mode|allowedChatIds|mapped Feishu user|projectId|active run|approve|批准|terminal input|redacted text summary|pending action count" docs/API.md</verify>
  <acceptance_criteria>
    - `docs/API.md` contains `allowedChatIds`.
    - `docs/API.md` contains `mapped Feishu user`.
    - `docs/API.md` contains `terminal input` in the public webhook non-goals text.
    - `docs/API.md` contains `redacted text summary`.
  </acceptance_criteria>
  <done>Fail-closed policy and non-goals are documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "webhook/:publicId|X-Lark-Request-Timestamp|X-Lark-Signature|single-Gateway|shared replay|mapped Feishu user|terminal input" docs/API.md`
- [ ] `rg -n "token|signature|encrypt key|raw Feishu message" docs/API.md` returns only documentation-policy language, not real secret values.
- [ ] `git diff --check`
</verification>

<success_criteria>
- FSH-01 through FSH-04 are specified in `docs/API.md` before public webhook implementation.
- `docs/API.md` keeps `/inbound` and `/webhook/:publicId` semantics separate.
- The docs clearly state the local single-Gateway storage boundary and multi-instance shared-store requirement.
- No real secret, token, signature, encrypt key, or raw private Feishu content is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-02-public-feishu-webhook-safety/02-01-SUMMARY.md`
</output>
