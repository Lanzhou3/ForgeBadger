---
phase: 02-public-feishu-webhook-safety
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - packages/gateway/src/server.ts
  - packages/gateway/src/routes/index.ts
  - packages/gateway/src/routes/integrations-feishu.ts
  - packages/gateway/src/db/schema.ts
  - packages/gateway/src/db/migrations/0021_feishu_public_webhook.sql
  - packages/gateway/src/db/repositories/feishu-integration-repository.ts
  - packages/gateway/test/feishu-integration.test.ts
  - packages/gateway/test/copilot-routes.test.ts
  - docs/API.md
autonomous: true
requirements:
  - FSH-01
  - FSH-02
  - FSH-03
  - FSH-04
user_setup: []
must_haves:
  truths:
    - "D-01: Public webhook code is separate from the JWT-protected `/inbound` test adapter."
    - "D-02: Public webhook lookup uses a per-integration public id."
    - "D-03: Public webhook handling is disabled by default until tenant config is complete."
    - "D-04: The public route does not mix JWT auth and Feishu webhook auth in one handler."
    - "D-05: Ordinary events require Feishu timestamp, nonce, signature, and raw-body verification."
    - "D-06: Timestamp freshness is checked before event normalization and Copilot execution."
    - "D-07: URL verification challenge handling is setup-only and cannot trigger Copilot side effects."
    - "D-08: Encrypted payload support validates tenant token/encrypt-key settings before normalization."
    - "D-09: Replay protection uses a dedicated persistent store."
    - "D-10: Replay behavior is tested as restart-safe for local SQLite single-Gateway deployment."
    - "D-11: Multi-instance public webhook use stays out of scope without a shared store and documented fail-closed behavior."
    - "D-12: Public rate limits are repository-backed for tenant/integration, chat, and mapped-user scopes."
    - "D-13: Public events reuse the guarded inbound fail-closed tenant policy before creating Copilot runs."
    - "D-14: Unsupported Feishu event types are acknowledged without side effects after verification."
    - "D-15: Free-form approval text remains unsupported."
    - "D-16: Accepted events may create only Copilot `source: \"feishu\"` runs, not terminal/session writes."
    - "D-17: Signature, timestamp, decrypt, and token failures return minimal non-2xx responses and log no sensitive request material."
    - "D-18: Policy rejections after tenant resolution write redacted audit rows with reason codes."
    - "D-19: Accepted events write bounded audit metadata including ids, run/conversation linkage, and redacted text summary."
    - "D-20: Provider/model/API surfaces receive redacted Feishu context only."
  artifacts:
    - "packages/gateway/src/routes/integrations-feishu.ts"
    - "packages/gateway/src/db/repositories/feishu-integration-repository.ts"
    - "packages/gateway/src/db/migrations/0021_feishu_public_webhook.sql"
    - "packages/gateway/test/feishu-integration.test.ts"
    - "docs/API.md"
  key_links:
    - "Public route tests prove missing/invalid signature and stale timestamp cannot create Copilot runs."
    - "Replay/rate-limit tests prove state survives route recreation and does not rely on in-memory `Map`."
    - "Existing `/inbound` tests continue to pass unchanged."
---

<objective>
Implement and verify the public Feishu webhook route behind explicit configuration.

Purpose: public Feishu ingress must pass signature, timestamp, replay, rate-limit, tenant policy, redaction, and audit gates before any Copilot side effect.
Output: Gateway route, repository/migration support, tests, and docs for public Feishu webhook safety.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/OF-02-public-feishu-webhook-safety/02-CONTEXT.md
@.planning/phases/OF-02-public-feishu-webhook-safety/02-RESEARCH.md
@.planning/phases/OF-02-public-feishu-webhook-safety/02-PATTERNS.md
@.planning/phases/OF-02-public-feishu-webhook-safety/02-VALIDATION.md
@docs/API.md
@packages/gateway/src/server.ts
@packages/gateway/src/routes/index.ts
@packages/gateway/src/routes/integrations-feishu.ts
@packages/gateway/src/db/schema.ts
@packages/gateway/src/db/repositories/feishu-integration-repository.ts
@packages/gateway/test/feishu-integration.test.ts
@packages/gateway/test/copilot-routes.test.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Spoofing | Public webhook request auth | high | mitigate | Require `X-Lark-Request-Timestamp`, `X-Lark-Request-Nonce`, `X-Lark-Signature`, raw-body signature verification, and verification token/decrypt validation before event normalization. |
| T-02-05 | Replay | Public webhook event handling | high | mitigate | Add dedicated persistent replay entries keyed by tenant/integration and event/message/nonce keys; reject duplicate inserts atomically. |
| T-02-06 | Denial of service | Public webhook rate limiting | high | mitigate | Use repository-backed per integration/chat/user rate windows and acknowledge authentic policy rejects minimally when retrying would amplify load. |
| T-02-07 | Elevation of privilege | Feishu command semantics | high | mitigate | Reuse enabled/emergency/identity/allowlist/mapping/project/active-run checks and keep approval/terminal input impossible from free-form text. |
| T-02-08 | Information disclosure | Webhook response, audit, provider context | high | mitigate | Redact text and never persist or return raw signatures, tokens, encrypt keys, credentials, raw CLI stderr, or secret-like message content. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add public webhook failing tests before implementation</name>
  <files>packages/gateway/test/feishu-integration.test.ts</files>
  <read_first>packages/gateway/test/feishu-integration.test.ts, packages/gateway/src/routes/integrations-feishu.ts, docs/API.md, .planning/phases/OF-02-public-feishu-webhook-safety/02-CONTEXT.md</read_first>
  <action>Add tests for `POST /api/v1/integrations/feishu/webhook/:publicId`: disabled or unknown public id creates no run; `url_verification` challenge returns `{"challenge":"challenge-value"}` and creates no run; ordinary event missing `X-Lark-Request-Timestamp`, `X-Lark-Request-Nonce`, or `X-Lark-Signature` returns a non-2xx response and creates no run; stale timestamp outside 5 minutes returns non-2xx; valid signed allowed message creates one Feishu-sourced Copilot run; replaying the same event/message id creates no second run; rate limit blocks the second accepted message for the same chat/user; `/approve action-id` leaves pending action status `pending`. Tests must assert response/audit/model request text does not contain `sk-public-webhook-secret`.</action>
  <verify>pnpm --dir packages/gateway test test/feishu-integration.test.ts</verify>
  <acceptance_criteria>
    - `packages/gateway/test/feishu-integration.test.ts` contains `/webhook/:publicId` or `/webhook/`.
    - The new tests mention `X-Lark-Request-Timestamp`, `X-Lark-Request-Nonce`, and `X-Lark-Signature`.
    - Before implementation, at least one new public webhook test fails for a missing route or missing verification behavior.
  </acceptance_criteria>
  <done>Public webhook safety tests exist before implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Add webhook config, replay, and rate-limit persistence</name>
  <files>packages/gateway/src/db/schema.ts, packages/gateway/src/db/migrations/0021_feishu_public_webhook.sql, packages/gateway/src/db/repositories/feishu-integration-repository.ts</files>
  <read_first>packages/gateway/src/db/schema.ts, packages/gateway/src/db/migrations/0019_feishu_integration.sql, packages/gateway/src/db/repositories/feishu-integration-repository.ts, packages/gateway/src/db/repositories/api-key-repository.ts, packages/gateway/src/crypto/secret-box.ts</read_first>
  <action>Add migration `0021_feishu_public_webhook.sql` and matching Drizzle schema. Extend `integration_feishu_configs` with `public_webhook_id`, `public_webhook_enabled`, `verification_token_encrypted`, `event_encrypt_key_encrypted`, and `webhook_configured_at`. Add `integration_feishu_webhook_replay_entries` with `user_id`, `public_webhook_id`, `replay_key`, `expires_at`, `created_at`, and unique index on `user_id, public_webhook_id, replay_key`. Add `integration_feishu_webhook_rate_windows` with `user_id`, `public_webhook_id`, `scope`, `scope_id`, `window_started_at`, `count`, `updated_at`, and unique index on `user_id, public_webhook_id, scope, scope_id`. Add repository methods to generate/find public ids, store encrypted verification token/encrypt key with `encryptSecret`, decrypt only inside verification helpers, atomically consume replay keys, and consume rate windows.</action>
  <verify>pnpm --dir packages/gateway test test/feishu-integration.test.ts</verify>
  <acceptance_criteria>
    - `packages/gateway/src/db/migrations/0021_feishu_public_webhook.sql` exists.
    - `packages/gateway/src/db/schema.ts` contains `integrationFeishuWebhookReplayEntries`.
    - `packages/gateway/src/db/schema.ts` contains `integrationFeishuWebhookRateWindows`.
    - `packages/gateway/src/db/repositories/feishu-integration-repository.ts` imports `encryptSecret` or `decryptSecret`.
  </acceptance_criteria>
  <done>Public webhook config and protection state are persistent and tenant scoped.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Verify Drizzle migration alignment</name>
  <files>packages/gateway/src/db/schema.ts, packages/gateway/src/db/migrations/0021_feishu_public_webhook.sql</files>
  <read_first>packages/gateway/src/db/schema.ts, packages/gateway/src/db/migrations/0021_feishu_public_webhook.sql, packages/gateway/package.json</read_first>
  <action>After schema and migration edits are complete, run a migration-backed test command that creates a fresh in-memory database and applies all migrations. Use `pnpm --dir packages/gateway test test/feishu-integration.test.ts` as the required non-interactive schema verification. If `drizzle-kit` check/generate output shows drift, fix migration/schema before continuing. Do not mark the phase complete when migrations are untested.</action>
  <verify>pnpm --dir packages/gateway test test/feishu-integration.test.ts</verify>
  <acceptance_criteria>
    - The focused Feishu integration test exits 0 after applying migrations.
    - There is no `no such table` error for `integration_feishu_webhook_replay_entries`.
    - There is no `no such column` error for `public_webhook_id`.
  </acceptance_criteria>
  <done>Migration and schema are aligned before route verification.</done>
</task>

<task type="auto">
  <name>Task 4: Implement raw-body capture and public webhook verification</name>
  <files>packages/gateway/src/server.ts, packages/gateway/src/routes/integrations-feishu.ts, packages/gateway/src/routes/index.ts</files>
  <read_first>packages/gateway/src/server.ts, packages/gateway/src/routes/index.ts, packages/gateway/src/routes/integrations-feishu.ts, .planning/phases/OF-02-public-feishu-webhook-safety/02-CONTEXT.md</read_first>
  <action>Update `server.ts` so requests whose URL starts with `/api/v1/integrations/feishu/webhook/` retain raw body bytes for signature verification without logging or returning them. In `integrations-feishu.ts`, register `router.post("/webhook/:publicId", ...)` before `router.use(authenticate)`. Implement public id lookup; disabled/unknown config rejection; setup-only `url_verification` challenge response; ordinary event checks for timestamp, nonce, and signature; 5-minute timestamp freshness; SDK-compatible signature comparison with timing-safe comparison; encrypted payload handling only if verification token and event encrypt key are configured; minimal protocol responses. Keep authenticated config, mapping, status, and `/inbound` routes behind `router.use(authenticate)`.</action>
  <verify>pnpm --dir packages/gateway test test/feishu-integration.test.ts</verify>
  <acceptance_criteria>
    - `packages/gateway/src/routes/integrations-feishu.ts` contains `router.post("/webhook/:publicId"`.
    - `packages/gateway/src/routes/integrations-feishu.ts` registers the public webhook route before `router.use(authenticate)`.
    - `packages/gateway/src/routes/integrations-feishu.ts` contains `timingSafeEqual` or an equivalent timing-safe comparison helper.
    - Public webhook tests for missing signature, stale timestamp, and challenge handling pass.
  </acceptance_criteria>
  <done>Public webhook authenticity checks pass before event normalization.</done>
</task>

<task type="auto">
  <name>Task 5: Reuse guarded inbound policy after public verification</name>
  <files>packages/gateway/src/routes/integrations-feishu.ts, packages/gateway/test/feishu-integration.test.ts, packages/gateway/test/copilot-routes.test.ts</files>
  <read_first>packages/gateway/src/routes/integrations-feishu.ts, packages/gateway/src/db/repositories/copilot-repository.ts, packages/gateway/src/services/copilot/orchestrator.ts, packages/gateway/src/services/copilot/redaction.ts, packages/gateway/test/feishu-integration.test.ts, packages/gateway/test/copilot-routes.test.ts</read_first>
  <action>After public verification succeeds, normalize supported Feishu message events into the same bounded command shape as the guarded `/inbound` adapter and apply the same policy gates: enabled config, not emergency disabled, identity mode `user` or `bot`, non-empty explicit `allowedChatIds`, chat id in allowlist, mapped Feishu user equals tenant user, optional `projectId` visible to user, no active `queued/running/waiting_for_approval` run, repository-backed replay/rate limit, redaction, and audit. Accepted events call `CopilotOrchestrator.runText` with `source: "feishu"`. Free-form approval text and terminal input requests must not call approval routes or session input handlers.</action>
  <verify>pnpm --dir packages/gateway test test/feishu-integration.test.ts test/copilot-routes.test.ts</verify>
  <acceptance_criteria>
    - Valid public webhook message creates exactly one run with `source` equal to `feishu`.
    - Replayed public webhook event creates no second run.
    - Public webhook `/approve` text leaves the pending action status `pending`.
    - Existing authenticated `/api/v1/integrations/feishu/inbound` tests still pass.
  </acceptance_criteria>
  <done>Public webhook events reuse the existing fail-closed Copilot ingress policy.</done>
</task>

<task type="auto">
  <name>Task 6: Update docs and run full focused verification</name>
  <files>docs/API.md, packages/gateway/test/feishu-integration.test.ts, packages/gateway/test/copilot-routes.test.ts</files>
  <read_first>docs/API.md, .planning/phases/OF-02-public-feishu-webhook-safety/02-VALIDATION.md, packages/gateway/test/feishu-integration.test.ts, packages/gateway/test/copilot-routes.test.ts</read_first>
  <action>Update `docs/API.md` to match implemented field names, response examples, route path, failure semantics, and single-Gateway storage caveat. Run `pnpm --dir packages/gateway test test/feishu-integration.test.ts test/copilot-routes.test.ts`, `pnpm --dir packages/gateway typecheck`, and `git diff --check`. Run a targeted secret grep across changed docs/tests/code for `sk-public-webhook-secret`, `verification_token=`, `event_encrypt_key=`, `X-Lark-Signature:`, and `Bearer `; only placeholders or test dummy values may remain.</action>
  <verify>pnpm --dir packages/gateway test test/feishu-integration.test.ts test/copilot-routes.test.ts && pnpm --dir packages/gateway typecheck && git diff --check</verify>
  <acceptance_criteria>
    - Focused Gateway tests exit 0.
    - Gateway typecheck exits 0.
    - `git diff --check` exits 0.
    - `docs/API.md` contains the implemented public webhook route path.
  </acceptance_criteria>
  <done>Public webhook safety implementation and docs are verified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/gateway test test/feishu-integration.test.ts test/copilot-routes.test.ts`
- [ ] `pnpm --dir packages/gateway typecheck`
- [ ] `git diff --check`
- [ ] `rg -n "sk-public-webhook-secret|verification_token=|event_encrypt_key=|X-Lark-Signature:|Bearer " packages/gateway/src packages/gateway/test docs/API.md` shows no real secret material.
</verification>

<success_criteria>
- Public webhook route exists separately from authenticated `/inbound`.
- Ordinary events require Feishu timestamp, nonce, signature, raw-body verification, replay protection, and rate limiting before Copilot execution.
- Replay and rate limiting are persistent for local single-Gateway deployment and documented as requiring shared store before multi-instance use.
- Chat allowlist, identity mode, user mapping, redaction, audit, project ownership, and active-run behavior remain fail-closed.
- Natural-language Feishu approval text and direct terminal input cannot approve actions or write to terminal sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-02-public-feishu-webhook-safety/02-02-SUMMARY.md`
</output>
