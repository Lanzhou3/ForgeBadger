---
phase: 03-first-user-product-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/app/(dashboard)/page.tsx
  - packages/web/src/app/(dashboard)/settings/page.tsx
  - packages/web/src/app/(dashboard)/projects/[id]/page.tsx
  - packages/web/src/app/(dashboard)/sessions/[id]/page.tsx
  - packages/web/src/lib/terminal-runtime.ts
  - packages/web/src/lib/terminal-runtime.test.ts
  - packages/web/src/lib/i18n.ts
autonomous: true
requirements:
  - UX-01
  - UX-06
user_setup: []
must_haves:
  truths:
    - "D-01: Missing `tmux`, unsupported native Windows terminal runtime, and missing selected CLI runtime surface as visible actionable guidance in Dashboard, Settings, project launch, session open, and diagnostics-adjacent surfaces."
    - "D-02: Runtime guidance reuses Gateway dependency report and adapter discovery; `terminalRuntime.mode` and adapter `available` / `launchEnabled` remain the source of truth."
    - "D-03: Native Windows remains management-UI-only for terminal acceptance; WSL/tmux evidence is required before removing caveats."
    - "D-10: Settings and runtime panels prefer explicit degraded/error states over empty panels."
    - "D-11: Recoverable runtime errors include useful next actions such as open Settings, retry discovery, open Models, or ask Copilot from the current source context."
  artifacts:
    - "packages/web/src/lib/terminal-runtime.ts"
    - "packages/web/src/lib/terminal-runtime.test.ts"
    - "packages/web/src/app/(dashboard)/page.tsx"
    - "packages/web/src/app/(dashboard)/settings/page.tsx"
    - "packages/web/src/app/(dashboard)/projects/[id]/page.tsx"
    - "packages/web/src/app/(dashboard)/sessions/[id]/page.tsx"
  key_links:
    - "Dashboard dependency health does not report healthy when dependency query data is missing due to an error."
    - "Settings adapter discovery failures are visible and actionable."
    - "Project launch and session open states point at runtime/adapter remediation."
---

<objective>
Harden dependency and runtime failure states for first-user local trials.

Purpose: first users should understand whether OpenForge is blocked by missing `tmux`, native Windows terminal limitations, missing CLI runtime, adapter discovery failure, or session attach failure without reading logs first.
Output: Web runtime guidance helpers, visible degraded states, localized copy, and focused tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/OF-03-first-user-product-hardening/03-CONTEXT.md
@.planning/phases/OF-03-first-user-product-hardening/03-RESEARCH.md
@.planning/phases/OF-03-first-user-product-hardening/03-PATTERNS.md
@.planning/phases/OF-03-first-user-product-hardening/03-VALIDATION.md
@packages/gateway/src/lib/dependency-check.ts
@packages/web/src/lib/terminal-runtime.ts
@packages/web/src/app/(dashboard)/page.tsx
@packages/web/src/app/(dashboard)/settings/page.tsx
@packages/web/src/app/(dashboard)/projects/[id]/page.tsx
@packages/web/src/app/(dashboard)/sessions/[id]/page.tsx
@packages/web/src/lib/i18n.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Integrity | Runtime readiness UI | high | mitigate | Use Gateway dependency and adapter discovery data as source of truth; do not infer launchability from local labels. |
| T-03-02 | Information disclosure | Runtime/session error UI | medium | mitigate | Surface actionable generic remediation while avoiding JWT, attach token, path secret, or provider secret display. |
| T-03-03 | Integrity | Windows/tmux caveat | medium | mitigate | Preserve native Windows terminal caveat unless physical Windows/WSL evidence is attached in docs. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add runtime remediation helper and tests</name>
  <files>packages/web/src/lib/terminal-runtime.ts, packages/web/src/lib/terminal-runtime.test.ts, packages/web/src/lib/i18n.ts</files>
  <read_first>packages/web/src/lib/terminal-runtime.ts, packages/gateway/src/lib/dependency-check.ts, packages/web/src/lib/i18n.ts, .planning/phases/OF-03-first-user-product-hardening/03-PATTERNS.md</read_first>
  <action>Add exported helper data for runtime remediation keyed by `native_tmux`, `wsl_required`, `tmux_missing`, and unknown mode. Keep `terminalRuntimeTranslationKey(mode)` intact. Add helpers that return a severity/action translation key and route target for each mode: `native_tmux` is healthy, `wsl_required` points to trial runbook/Settings guidance, `tmux_missing` points to Settings/diagnostics guidance, unknown points to Settings. Add `packages/web/src/lib/terminal-runtime.test.ts` with table tests for all modes and no hardcoded secret-like output. Add required `i18n.ts` keys in zh-CN, zh-TW, and en.</action>
  <verify>pnpm --dir packages/web vitest run src/lib/terminal-runtime.test.ts</verify>
  <acceptance_criteria>
    - `packages/web/src/lib/terminal-runtime.ts` still exports `terminalRuntimeTranslationKey`.
    - `packages/web/src/lib/terminal-runtime.ts` contains cases for `native_tmux`, `wsl_required`, and `tmux_missing`.
    - `packages/web/src/lib/terminal-runtime.test.ts` exists and exits 0 with the focused Vitest command.
    - `packages/web/src/lib/i18n.ts` contains runtime remediation keys for all three supported languages.
  </acceptance_criteria>
  <done>Runtime remediation has a tested helper instead of page-local conditionals.</done>
</task>

<task type="auto">
  <name>Task 2: Make Dashboard dependency health fail visibly</name>
  <files>packages/web/src/app/(dashboard)/page.tsx, packages/web/src/lib/i18n.ts</files>
  <read_first>packages/web/src/app/(dashboard)/page.tsx, packages/web/src/lib/terminal-runtime.ts, packages/web/src/lib/i18n.ts</read_first>
  <action>Update Dashboard dependency health so `dependenciesQuery.isError` or missing `terminalRuntime` is not treated as healthy. Use the runtime remediation helper for detail text and action route. Preserve the existing health card layout and Copilot launch link. Add localized text that distinguishes loading, unsupported native Windows/WSL required, tmux missing, and dependency status unavailable.</action>
  <verify>pnpm --dir packages/web typecheck</verify>
  <acceptance_criteria>
    - `packages/web/src/app/(dashboard)/page.tsx` does not use `terminalRuntime?.supported ?? !dependenciesQuery.isError` as the final healthy expression.
    - Dashboard dependency card still links to `/settings`.
    - `pnpm --dir packages/web typecheck` exits 0.
  </acceptance_criteria>
  <done>Dashboard no longer false-greens unavailable dependency status.</done>
</task>

<task type="auto">
  <name>Task 3: Add Settings adapter discovery degraded state</name>
  <files>packages/web/src/app/(dashboard)/settings/page.tsx, packages/web/src/lib/i18n.ts</files>
  <read_first>packages/web/src/app/(dashboard)/settings/page.tsx, packages/web/src/lib/i18n.ts, .planning/phases/OF-03-first-user-product-hardening/03-PATTERNS.md</read_first>
  <action>Extend the Settings adapter discovery query to track `isError` and `error`. In the Adapters card, mirror the Feishu error branch pattern: loading message, visible load-failed message, retry button using query refetch, and a short remediation line that tells users CLI launch readiness depends on local `claude`, `opencode`, or `codex` commands plus tmux. Do not display raw error stacks or secrets.</action>
  <verify>pnpm --dir packages/web typecheck</verify>
  <acceptance_criteria>
    - `packages/web/src/app/(dashboard)/settings/page.tsx` reads `isError` for the adapter discovery query.
    - Settings Adapters card displays a localized load-failed message when adapter discovery fails.
    - The retry control calls the adapter discovery query refetch or invalidates the query.
    - `pnpm --dir packages/web typecheck` exits 0.
  </acceptance_criteria>
  <done>Settings adapter failures are visible and recoverable.</done>
</task>

<task type="auto">
  <name>Task 4: Make project/session runtime blockers actionable</name>
  <files>packages/web/src/app/(dashboard)/projects/[id]/page.tsx, packages/web/src/app/(dashboard)/sessions/[id]/page.tsx, packages/web/src/lib/i18n.ts</files>
  <read_first>packages/web/src/app/(dashboard)/projects/[id]/page.tsx, packages/web/src/app/(dashboard)/sessions/[id]/page.tsx, packages/web/src/lib/session-connect-state.ts, packages/web/src/lib/i18n.ts</read_first>
  <action>In project launch options, expand the no-launchable-runtime message to include a Settings link and Copilot project-readiness link while preserving disabled adapter options. In session open fallback, add a Settings link and Copilot session-readiness link next to Back to Sessions. Keep attach token/JWT values out of UI. Do not change session connect API semantics.</action>
  <verify>pnpm --dir packages/web vitest run src/lib/session-connect-state.test.ts && pnpm --dir packages/web typecheck</verify>
  <acceptance_criteria>
    - Project launch options still disable unavailable adapters using `isAdapterLaunchable`.
    - Project no-launchable-runtime guidance contains a link to `/settings` or existing Settings route.
    - Session open failure UI contains Back to Sessions plus a recovery link to Settings or Copilot.
    - Focused session-connect-state Vitest and Web typecheck exit 0.
  </acceptance_criteria>
  <done>Runtime blockers point first users to recovery paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/web vitest run src/lib/terminal-runtime.test.ts src/lib/session-connect-state.test.ts`
- [ ] `pnpm --dir packages/web typecheck`
- [ ] `rg -n "OPENFORGE|JWT|attach token|sk-" packages/web/src/app/(dashboard)/page.tsx packages/web/src/app/(dashboard)/settings/page.tsx packages/web/src/app/(dashboard)/projects/[id]/page.tsx packages/web/src/app/(dashboard)/sessions/[id]/page.tsx packages/web/src/lib/terminal-runtime.ts packages/web/src/lib/i18n.ts` shows no secret values, only safe UI copy if matches exist.
- [ ] `git diff --check`
</verification>

<success_criteria>
- Missing `tmux`, native Windows WSL requirement, unavailable selected CLI, adapter discovery failure, and session connect failure have visible recovery guidance.
- Runtime readiness uses Gateway dependency report and adapter discovery as authority.
- Settings partial failures no longer render as empty panels.
- New user-facing text is localized and does not expose secret values.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-03-first-user-product-hardening/03-01-SUMMARY.md`
</output>
