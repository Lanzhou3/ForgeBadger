---
phase: 03-first-user-product-hardening
plan: 04
type: execute
wave: 3
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - packages/web/e2e/models.spec.ts
  - packages/web/e2e/copilot.spec.ts
  - packages/web/src/lib/copilot.test.ts
autonomous: true
requirements:
  - UX-03
  - UX-05
  - UX-07
user_setup: []
must_haves:
  truths:
    - "D-07: E2E/state tests preserve monotonic Copilot active-run behavior."
    - "D-09: Stale async request behavior is tested through deterministic response ordering."
    - "D-14: Touched E2E `/api/v1/*` mocks fail fast for unhandled routes."
    - "D-15: Critical provider/Copilot assertions use stable selectors, accessible names, or helper locators instead of brittle article-role/long-copy selectors."
    - "D-16: Test credentials are clearly fake and do not look like live secrets except intentional redacted `secretPreview` contract values."
  artifacts:
    - "packages/web/e2e/models.spec.ts"
    - "packages/web/e2e/copilot.spec.ts"
    - "packages/web/src/lib/copilot.test.ts"
  key_links:
    - "Models E2E mock fallback returns HTTP 404 for unhandled `/api/v1/*` routes."
    - "Copilot strict fallback remains in place after adding Phase 3 tests."
---

<objective>
Harden Web E2E mocks, selectors, and Copilot state ordering regression signal.

Purpose: browser tests should fail when contracts drift, and critical Copilot/provider tests should not depend on fragile selectors or permissive mock fallbacks.
Output: strict Models E2E fallback, preserved Copilot strict fallback, targeted selector cleanup, and focused state-ordering assertions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/OF-03-first-user-product-hardening/03-CONTEXT.md
@.planning/phases/OF-03-first-user-product-hardening/03-RESEARCH.md
@.planning/phases/OF-03-first-user-product-hardening/03-PATTERNS.md
@.planning/phases/OF-03-first-user-product-hardening/03-VALIDATION.md
@packages/web/e2e/models.spec.ts
@packages/web/e2e/copilot.spec.ts
@packages/web/src/lib/copilot.test.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Integrity | Web E2E mocks | high | mitigate | Replace success fallback with HTTP 404 fail-fast behavior for unhandled `/api/v1/*` routes in touched specs. |
| T-03-11 | Integrity | E2E selectors | medium | mitigate | Prefer stable selectors/accessibility locators for critical assertions to avoid false failures or false passes. |
| T-03-12 | Information disclosure | E2E fixture credentials | medium | mitigate | Keep entered credential values fake and use `secretPreview` only as redacted contract output. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Make Models E2E API fallback fail fast</name>
  <files>packages/web/e2e/models.spec.ts</files>
  <read_first>packages/web/e2e/models.spec.ts, packages/web/e2e/copilot.spec.ts, .planning/phases/OF-03-first-user-product-hardening/03-PATTERNS.md</read_first>
  <action>Replace the final `await route.fulfill({ json: envelope({}) })` in `mockModelsApis()` with the Copilot strict fallback pattern: HTTP 404, `{ code: 1, message: \`Unhandled mocked API route: ${method} ${url.pathname}\` }`. Run the focused Models E2E and update explicit mock handlers for any route that was unintentionally relying on the permissive fallback.</action>
  <verify>pnpm --dir packages/web exec playwright test e2e/models.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - `packages/web/e2e/models.spec.ts` contains `Unhandled mocked API route`.
    - The final fallback in `mockModelsApis()` uses `status: 404`.
    - There is no final `route.fulfill({ json: envelope({}) })` fallback for `/api/v1/**`.
    - Focused Models Playwright exits 0, or an environment-gated skip reason is recorded in the summary.
  </acceptance_criteria>
  <done>Models E2E no longer hides unhandled API contract drift.</done>
</task>

<task type="auto">
  <name>Task 2: Audit Copilot and Models selectors for critical flows</name>
  <files>packages/web/e2e/models.spec.ts, packages/web/e2e/copilot.spec.ts</files>
  <read_first>packages/web/e2e/models.spec.ts, packages/web/e2e/copilot.spec.ts</read_first>
  <action>Review critical assertions for provider setup, credential/model save, Copilot message rendering, pending actions, provider blockers, and stale-state tests. Replace brittle `getByRole("article").filter({ hasText: ... })` or long exact-copy selectors in touched tests with existing helper locators, `data-testid`, form labels, button accessible names, or shorter semantic text. Do not mass-rewrite unrelated assertions.</action>
  <verify>rg -n "getByRole\\(\"article\"|hasText: `|hasText: \"" packages/web/e2e/models.spec.ts packages/web/e2e/copilot.spec.ts</verify>
  <acceptance_criteria>
    - Critical Phase 3 assertions added or touched in `copilot.spec.ts` use helper `copilotMessage`, `data-testid`, or accessible names.
    - No touched test uses `getByRole("article")` for Copilot message assertions.
    - Existing unrelated selectors are left alone unless they block focused tests.
  </acceptance_criteria>
  <done>Critical Phase 3 E2E selectors are stable enough for regression signal.</done>
</task>

<task type="auto">
  <name>Task 3: Add state-ordering regression checks where E2E cannot prove enough</name>
  <files>packages/web/src/lib/copilot.test.ts, packages/web/e2e/copilot.spec.ts</files>
  <read_first>packages/web/src/lib/copilot.test.ts, packages/web/e2e/copilot.spec.ts, packages/web/src/lib/copilot.ts</read_first>
  <action>If Plan 03-02 did not already cover these exact cases, add deterministic unit tests for request-order/stale-response behavior and one Copilot E2E test that simulates out-of-order `GET /api/v1/copilot/runs/run-1` responses through `overrides.onGetRun`. The expected visible result is that terminal/cancelled/completed/waiting state does not regress to stale running state.</action>
  <verify>pnpm --dir packages/web vitest run src/lib/copilot.test.ts && pnpm --dir packages/web exec playwright test e2e/copilot.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - `packages/web/src/lib/copilot.test.ts` covers stale response/request ordering if not already covered by 03-02.
    - `packages/web/e2e/copilot.spec.ts` has at least one deterministic stale run response case.
    - Focused Copilot Vitest exits 0.
    - Focused Copilot Playwright exits 0, or an environment-gated skip reason is recorded in the summary.
  </acceptance_criteria>
  <done>Copilot ordering has both helper-level and browser-level regression signal.</done>
</task>

<task type="auto">
  <name>Task 4: Run focused Web verification and secret scan</name>
  <files>packages/web/e2e/models.spec.ts, packages/web/e2e/copilot.spec.ts, packages/web/src/lib/copilot.test.ts</files>
  <read_first>packages/web/e2e/models.spec.ts, packages/web/e2e/copilot.spec.ts, packages/web/src/lib/copilot.test.ts, .planning/phases/OF-03-first-user-product-hardening/03-VALIDATION.md</read_first>
  <action>Run focused Web unit/typecheck/E2E verification: `pnpm --dir packages/web vitest run src/lib/copilot.test.ts`, `pnpm --dir packages/web typecheck`, `pnpm --dir packages/web exec playwright test e2e/models.spec.ts e2e/copilot.spec.ts --project=chromium`, and `git diff --check`. If Playwright cannot run due loopback/browser sandbox, record the exact error in the plan summary and do not claim E2E pass. Run a changed-file grep for `sk-minimax-test`, `sk-`, `Bearer `, `Authorization`, `JWT`, and `attach token`; only redacted preview strings or safety-warning text may remain.</action>
  <verify>pnpm --dir packages/web vitest run src/lib/copilot.test.ts && pnpm --dir packages/web typecheck && git diff --check</verify>
  <acceptance_criteria>
    - Focused Copilot Vitest exits 0.
    - Web typecheck exits 0.
    - `git diff --check` exits 0.
    - Playwright result or explicit environment-gated skip reason is recorded in `03-04-SUMMARY.md`.
  </acceptance_criteria>
  <done>Web E2E hardening has verification evidence and no secret-like fixture regression.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/web vitest run src/lib/copilot.test.ts`
- [ ] `pnpm --dir packages/web typecheck`
- [ ] `pnpm --dir packages/web exec playwright test e2e/models.spec.ts e2e/copilot.spec.ts --project=chromium` or explicit environment-gated caveat
- [ ] `rg -n "await route.fulfill\\(\\{ json: envelope\\(\\{\\}\\) \\}\\)|sk-minimax-test|Bearer |Authorization" packages/web/e2e/models.spec.ts packages/web/e2e/copilot.spec.ts`
- [ ] `git diff --check`
</verification>

<success_criteria>
- Touched `/api/v1/*` E2E mocks fail fast for unhandled routes.
- Critical Copilot/provider assertions use stable selectors where practical.
- Copilot state-ordering regression has deterministic test coverage.
- Test credentials are clearly fake/redacted and no real secrets are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-03-first-user-product-hardening/03-04-SUMMARY.md`
</output>
