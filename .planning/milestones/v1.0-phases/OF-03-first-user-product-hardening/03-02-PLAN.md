---
phase: 03-first-user-product-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - packages/web/src/lib/copilot.ts
  - packages/web/src/lib/copilot.test.ts
  - packages/web/src/components/copilot/copilot-chat-panel.tsx
  - packages/web/src/lib/i18n.ts
  - packages/web/e2e/copilot.spec.ts
autonomous: true
requirements:
  - UX-02
  - UX-03
  - UX-05
  - UX-06
user_setup: []
must_haves:
  truths:
    - "D-04: Provider recovery points to the exact missing layer or provider failure class."
    - "D-05: Recovery surfaces never expose plaintext provider secrets, JWTs, attach tokens, or raw provider request payloads."
    - "D-06: Codex remains subscription/SDK-managed and is not routed through provider API-key/model setup."
    - "D-07: Copilot Web active-run updates are monotonic and stale poll/gateway responses cannot overwrite newer terminal states, higher event sequences, or newer pending-action data."
    - "D-08: `waiting_for_approval` remains live and coherent through refresh, approval, rejection, cancellation, and multiple tabs."
    - "D-09: Stale async requests are guarded by helper-level freshness/request-order checks, not timing assumptions."
    - "D-10: Copilot partial failures are visible instead of empty panels."
    - "D-11: Copilot recovery states include next useful actions such as retry, open Models, or export diagnostics where practical."
  artifacts:
    - "packages/web/src/lib/copilot.ts"
    - "packages/web/src/lib/copilot.test.ts"
    - "packages/web/src/components/copilot/copilot-chat-panel.tsx"
    - "packages/web/e2e/copilot.spec.ts"
  key_links:
    - "All active-run state updates still flow through `applyActiveRunState()` or a stronger helper."
    - "Focused tests prove stale running/queued responses cannot overwrite cancelled/completed/failed/waiting states."
---

<objective>
Harden provider/Copilot recovery and Copilot state clarity.

Purpose: first users should see precise provider recovery guidance and Copilot run state should remain coherent when requests return out of order, the page refreshes, or a run waits for approval.
Output: helper-level stale-response guards, component wiring, localized recovery copy, and focused unit/E2E tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/OF-03-first-user-product-hardening/03-CONTEXT.md
@.planning/phases/OF-03-first-user-product-hardening/03-RESEARCH.md
@.planning/phases/OF-03-first-user-product-hardening/03-PATTERNS.md
@.planning/phases/OF-03-first-user-product-hardening/03-VALIDATION.md
@packages/web/src/lib/copilot.ts
@packages/web/src/lib/copilot.test.ts
@packages/web/src/components/copilot/copilot-chat-panel.tsx
@packages/web/e2e/copilot.spec.ts
@packages/web/src/lib/i18n.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Integrity | Copilot active-run UI | high | mitigate | Add tests and helper semantics so stale async responses cannot regress terminal/cancel/approval state. |
| T-03-05 | Information disclosure | Provider/Copilot errors | high | mitigate | Map provider failures to redacted user-facing categories; never render raw secrets, headers, provider payloads, JWTs, or attach tokens. |
| T-03-06 | Repudiation | Pending actions | medium | mitigate | Preserve visible `waiting_for_approval` and pending-action state across refresh/decision paths. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen Copilot active-run freshness tests</name>
  <files>packages/web/src/lib/copilot.ts, packages/web/src/lib/copilot.test.ts</files>
  <read_first>packages/web/src/lib/copilot.ts, packages/web/src/lib/copilot.test.ts, .planning/phases/OF-03-first-user-product-hardening/03-PATTERNS.md</read_first>
  <action>Add focused tests for `shouldKeepCopilotActiveRunState` and any new helper needed to cover: terminal `cancelled`/`failed`/`completed` not overwritten by stale `running`; `waiting_for_approval` treated as live; pending-action `updatedAt` freshness wins at equal run timestamp; event sequence freshness wins at equal run timestamp; different run id accepts the next state. Implement helper changes only as required by tests. Keep exported names stable where possible.</action>
  <verify>pnpm --dir packages/web vitest run src/lib/copilot.test.ts</verify>
  <acceptance_criteria>
    - `packages/web/src/lib/copilot.test.ts` has assertions for stale `running` versus terminal status.
    - `packages/web/src/lib/copilot.test.ts` has assertions for pending-action `updatedAt` freshness.
    - `packages/web/src/lib/copilot.test.ts` has assertions that `waiting_for_approval` is live.
    - Focused Copilot Vitest exits 0.
  </acceptance_criteria>
  <done>Copilot run freshness behavior is covered before component changes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply request-order guard to Copilot panel async paths</name>
  <files>packages/web/src/components/copilot/copilot-chat-panel.tsx, packages/web/src/lib/copilot.ts, packages/web/src/lib/copilot.test.ts</files>
  <read_first>packages/web/src/components/copilot/copilot-chat-panel.tsx, packages/web/src/lib/copilot.ts, packages/web/src/lib/copilot.test.ts</read_first>
  <action>Ensure send success, approval/rejection success, polling, and Gateway event fetch paths either call `applyActiveRunState()` or a stronger shared helper that drops stale responses by run id, timestamp, event sequence, pending-action freshness, and request order. Add a monotonically increasing request token/ref for run-detail fetches if helper freshness alone cannot detect out-of-order same-version responses. Do not clear active run for an unrelated stale response. Preserve intentional `setActiveRun(null)` when selecting a different conversation or starting a new draft.</action>
  <verify>pnpm --dir packages/web vitest run src/lib/copilot.test.ts && pnpm --dir packages/web typecheck</verify>
  <acceptance_criteria>
    - `copilot-chat-panel.tsx` active-run detail fetches are guarded by `applyActiveRunState()` or a named stale-response helper.
    - `copilot-chat-panel.tsx` still clears active run in `startNewConversation()` and `selectConversation()` only for explicit conversation changes.
    - Focused Copilot Vitest and Web typecheck exit 0.
  </acceptance_criteria>
  <done>Async Copilot panel responses cannot regress active-run state.</done>
</task>

<task type="auto">
  <name>Task 3: Make provider recovery actions precise and redacted</name>
  <files>packages/web/src/components/copilot/copilot-chat-panel.tsx, packages/web/src/lib/copilot.ts, packages/web/src/lib/i18n.ts</files>
  <read_first>packages/web/src/components/copilot/copilot-chat-panel.tsx, packages/web/src/lib/copilot.ts, packages/web/src/lib/i18n.ts, docs/reports/platform-ai-copilot-product-audit-2026-05-13.md</read_first>
  <action>Review existing provider readiness and error mapping. If the panel only shows generic provider setup text when capability details expose no compatible provider, missing credential, or missing model, render the exact existing localized readiness message and a Models link. For run failures, keep using `resolveCopilotRunFailureMessage()` / `getCopilotErrorMessageKey()` and add retry/open Models/export diagnostics actions only when they do not reveal raw provider payloads. Do not add Codex provider/API-key recovery copy.</action>
  <verify>pnpm --dir packages/web typecheck</verify>
  <acceptance_criteria>
    - Copilot panel can render a Models link for provider setup blockers.
    - User-facing errors do not render raw provider request bodies, Authorization headers, JWTs, attach tokens, or plaintext secrets.
    - `packages/web/src/lib/i18n.ts` contains any new provider recovery labels in zh-CN, zh-TW, and en.
    - Web typecheck exits 0.
  </acceptance_criteria>
  <done>Provider/Copilot recovery directs users to the missing layer without leaking secrets.</done>
</task>

<task type="auto">
  <name>Task 4: Add focused Copilot E2E coverage for stale state and partial failures</name>
  <files>packages/web/e2e/copilot.spec.ts</files>
  <read_first>packages/web/e2e/copilot.spec.ts, packages/web/src/components/copilot/copilot-chat-panel.tsx, packages/web/src/lib/copilot.ts</read_first>
  <action>Add or extend Copilot E2E cases using the existing strict `mockCopilotApis` fallback. Cover at least one stale run-detail sequence where a terminal or `waiting_for_approval` state is followed by an older live response and the UI does not regress. Cover visible message-load or capability-load failure if not already covered. Use stable selectors such as `data-testid`, accessible names, or existing helper `copilotMessage()`; avoid article-role plus exact long-copy selectors.</action>
  <verify>pnpm --dir packages/web exec playwright test e2e/copilot.spec.ts --project=chromium</verify>
  <acceptance_criteria>
    - `packages/web/e2e/copilot.spec.ts` contains a stale response or out-of-order run detail test.
    - The mock fallback at the end of `mockCopilotApis` remains HTTP 404 for unhandled `/api/v1/*`.
    - Focused Copilot Playwright exits 0, or an environment-gated skip reason is recorded in the summary.
  </acceptance_criteria>
  <done>Copilot state clarity has browser-level regression signal.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/web vitest run src/lib/copilot.test.ts`
- [ ] `pnpm --dir packages/web typecheck`
- [ ] `pnpm --dir packages/web exec playwright test e2e/copilot.spec.ts --project=chromium` or explicit environment-gated caveat
- [ ] `rg -n "Authorization|Bearer |sk-|attach token|JWT" packages/web/src/components/copilot packages/web/src/lib/copilot.ts packages/web/e2e/copilot.spec.ts` shows no real secret material.
- [ ] `git diff --check`
</verification>

<success_criteria>
- Provider readiness and provider failures are recoverable from user-facing Copilot states without exposing secrets.
- Copilot active-run state remains monotonic under retries, refresh, pending approval, cancellation, and out-of-order responses.
- `waiting_for_approval` remains live and visible until approval/rejection/cancellation resolves.
- Copilot partial failures are visible rather than rendered as empty chat.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-03-first-user-product-hardening/03-02-SUMMARY.md`
</output>
