---
phase: 09-project-manager-web-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/lib/api.ts
  - packages/web/src/lib/api.test.ts
autonomous: true
requirements: [PMAPI-01, PMAPI-02]
requirements_addressed: [PMAPI-01, PMAPI-02]
user_setup: []
must_haves:
  truths:
    - "D-04: Project-manager DTOs and client functions live in `packages/web/src/lib/api.ts`."
    - "D-05: Project IDs and work item IDs are encoded before path interpolation."
    - "D-06: Web code must call Gateway endpoints and must not add Next.js API routes or duplicate Gateway validation."
    - "D-07: Client coverage includes every existing project-manager endpoint, including mutations used more deeply in later phases."
    - "D-11: Focused API tests must cover URL shape, HTTP method, request body, query params, and error propagation."
  artifacts:
    - "packages/web/src/lib/api.ts"
    - "packages/web/src/lib/api.test.ts"
  key_links:
    - "`docs/API.md` remains the REST contract source of truth."
    - "`packages/gateway/src/routes/project-manager.ts` remains the authoritative implementation shape."
---

<objective>
Implement the typed Web API client foundation for Project Manager Ledger routes.

Purpose: PMAPI-01 needs typed Web access to every existing Gateway
project-manager endpoint before the UI can safely consume project-manager
state.
Output: `packages/web/src/lib/api.ts` exports bounded DTOs and helper
functions for project goal, work items, status updates, evidence attachment,
and ledger reads, with focused tests in `packages/web/src/lib/api.test.ts`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OF-09-project-manager-web-foundation/09-CONTEXT.md
@.planning/phases/OF-09-project-manager-web-foundation/09-RESEARCH.md
@.planning/phases/OF-09-project-manager-web-foundation/09-PATTERNS.md
@.planning/phases/OF-09-project-manager-web-foundation/09-VALIDATION.md
@docs/API.md
@packages/gateway/src/routes/project-manager.ts
@packages/gateway/src/db/repositories/project-manager-repository.ts
@packages/web/src/lib/api.ts
@packages/web/src/lib/api.test.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Spoofing | Project-scoped client paths | high | mitigate | Encode `projectId` and `workItemId` before interpolation and call only Gateway `/api/v1/projects/:projectId/project-manager` routes. |
| T-09-02 | Information disclosure | Project-manager DTOs | high | mitigate | Expose only bounded DTO fields returned by Gateway; do not add raw ledger details, raw evidence blobs, terminal transcripts, provider payloads, or secrets. |
| T-09-03 | Integrity | Request bodies and query params | high | mitigate | Mirror Gateway input schemas and add tests that fail on wrong methods, bodies, query param names, or missing error propagation. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add typed project-manager API helpers</name>
  <files>packages/web/src/lib/api.ts</files>
  <read_first>docs/API.md, packages/gateway/src/routes/project-manager.ts, packages/gateway/src/db/repositories/project-manager-repository.ts, packages/web/src/lib/api.ts, .planning/phases/OF-09-project-manager-web-foundation/09-CONTEXT.md, .planning/phases/OF-09-project-manager-web-foundation/09-RESEARCH.md</read_first>
  <action>Add exported project-manager DTOs and inputs to `packages/web/src/lib/api.ts`: `ProjectManagerEvidenceRef`, `ProjectManagerGoal`, `ProjectManagerGoalInput`, `ProjectManagerWorkItemStatus`, `ProjectManagerWorkItem`, `ProjectManagerWorkItemInput`, `ProjectManagerWorkItemStatusInput`, `ProjectManagerEvidenceInput`, `ProjectManagerLedgerEventType`, and `ProjectManagerLedgerEvent`. Add exported helper functions for every existing route: `getProjectManagerGoal`, `updateProjectManagerGoal`, `listProjectManagerWorkItems`, `createProjectManagerWorkItem`, `getProjectManagerWorkItem`, `updateProjectManagerWorkItemStatus`, `attachProjectManagerWorkItemEvidence`, and `listProjectManagerLedger`. Use `fetchJson`, preserve the canonical envelope behavior, encode project/work item IDs, omit undefined query params, and keep request body field names aligned with Gateway schemas.</action>
  <verify>
    <automated>rg -n "ProjectManagerGoal|ProjectManagerWorkItem|ProjectManagerLedgerEvent|getProjectManagerGoal|updateProjectManagerGoal|listProjectManagerWorkItems|createProjectManagerWorkItem|getProjectManagerWorkItem|updateProjectManagerWorkItemStatus|attachProjectManagerWorkItemEvidence|listProjectManagerLedger" packages/web/src/lib/api.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/web/src/lib/api.ts` exports project-manager DTOs and input types.
    - The client exposes a helper for every existing Gateway project-manager route.
    - Every helper uses `fetchJson`.
    - `projectId` and `workItemId` path segments are encoded.
    - Work item and ledger query params use Gateway names and omit undefined values.
    - No Next.js API route or Gateway route is added by this task.
  </acceptance_criteria>
  <done>The Web layer has typed project-manager API helpers for all existing Gateway endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Cover project-manager helpers with focused API tests</name>
  <files>packages/web/src/lib/api.test.ts</files>
  <read_first>packages/web/src/lib/api.test.ts, packages/web/src/lib/api.ts, docs/API.md, packages/gateway/src/routes/project-manager.ts, .planning/phases/OF-09-project-manager-web-foundation/09-VALIDATION.md</read_first>
  <action>Extend `packages/web/src/lib/api.test.ts` with a focused project-manager client test group. Import the new helpers. Mock successful envelopes and assert each helper calls the exact expected URL, method, query params, and JSON body. Include an encoded ID case for a project ID or work item ID containing path-reserved characters. Include at least one rejected Gateway envelope or HTTP error case that proves errors propagate through the existing API client error path.</action>
  <verify>
    <automated>pnpm --dir packages/web vitest run src/lib/api.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The test group covers goal read/update, work item list/create/detail/status/evidence, and ledger list.
    - Tests assert HTTP methods and request bodies for all mutation helpers.
    - Tests assert query string names for work item and ledger filters.
    - Tests assert path encoding for project or work item identifiers.
    - Tests prove Gateway/API errors remain visible to callers instead of being swallowed.
  </acceptance_criteria>
  <done>The typed project-manager API client has focused regression coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/web vitest run src/lib/api.test.ts`
- [ ] `git diff --check`
- [ ] `rg -n "ProjectManagerGoal|ProjectManagerWorkItem|ProjectManagerLedgerEvent|getProjectManagerGoal|listProjectManagerWorkItems|listProjectManagerLedger" packages/web/src/lib/api.ts packages/web/src/lib/api.test.ts`
</verification>

<success_criteria>
- PMAPI-01 is covered by exported typed client functions for every existing project-manager Gateway endpoint.
- PMAPI-02 is supported at the client boundary by preserving Gateway error propagation for UI callers.
- API tests fail on wrong project-manager route, method, body, query param, ID encoding, or error behavior.
- No Gateway authority model or Next.js API route is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-09-project-manager-web-foundation/09-01-SUMMARY.md`.
</output>
