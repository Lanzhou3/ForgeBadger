---
phase: 09-project-manager-web-foundation
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - packages/web/src/app/(dashboard)/projects/[id]/page.tsx
  - packages/web/src/components/projects/ProjectManagerPanel.tsx
  - packages/web/src/lib/i18n.ts
  - packages/web/e2e/project-manager.spec.ts
autonomous: true
requirements: [PMAPI-02, PMUX-01]
requirements_addressed: [PMAPI-02, PMUX-01]
user_setup: []
must_haves:
  truths:
    - "D-01: The project-manager surface lives inside the existing project detail workflow as a `project-manager` tab."
    - "D-03: The first visible shape is an operational control panel using existing dense project page style."
    - "D-08: Project-manager data loads only when the project ID exists and the tab is active."
    - "D-09: Loading, empty goal, empty work item, API validation/error, and not-found states are visible."
    - "D-10: Phase 9 does not imply full editing, status transition, evidence attachment, or ledger filtering workflows are complete."
    - "D-12: Focused Web tests use strict `/api/v1/*` mocks that fail unknown project-manager routes."
  artifacts:
    - "packages/web/src/app/(dashboard)/projects/[id]/page.tsx"
    - "packages/web/src/components/projects/ProjectManagerPanel.tsx"
    - "packages/web/src/lib/i18n.ts"
    - "packages/web/e2e/project-manager.spec.ts"
  key_links:
    - "`09-UI-SPEC.md` defines copy, layout, spacing, typography, and interaction contract."
    - "`09-01-PLAN.md` provides the typed client helpers consumed by this plan."
---

<objective>
Add the first-class Project Manager surface to the project detail Web workflow.

Purpose: PMUX-01 needs project-manager state visible from project context, and
PMAPI-02 needs API loading/empty/error states to be visible instead of
collapsing into blank UI.
Output: A `Project Manager` tab in the project detail page backed by typed
client calls, translated copy, explicit states, and strict Playwright E2E
coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OF-09-project-manager-web-foundation/09-CONTEXT.md
@.planning/phases/OF-09-project-manager-web-foundation/09-RESEARCH.md
@.planning/phases/OF-09-project-manager-web-foundation/09-PATTERNS.md
@.planning/phases/OF-09-project-manager-web-foundation/09-VALIDATION.md
@.planning/phases/OF-09-project-manager-web-foundation/09-UI-SPEC.md
@packages/web/src/lib/api.ts
@packages/web/src/app/(dashboard)/projects/[id]/page.tsx
@packages/web/src/lib/i18n.ts
@packages/web/e2e/copilot.spec.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Integrity | Project Manager tab states | high | mitigate | Render explicit loading, empty, not-found, validation/error, and retry states; never show blank content after an API failure. |
| T-09-05 | Integrity | User workflow authority | high | mitigate | Keep Phase 10/11 mutation affordances absent or disabled and clearly marked so users do not think unsupported writes are complete. |
| T-09-06 | Information disclosure | Project Manager summaries | high | mitigate | Show safe fields, counts, statuses, and timestamps only; do not surface raw ledger details, terminal transcripts, provider payloads, Feishu secrets, or raw evidence blobs. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add the Project Manager tab and panel</name>
  <files>packages/web/src/app/(dashboard)/projects/[id]/page.tsx, packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/app/(dashboard)/projects/[id]/page.tsx, packages/web/src/lib/api.ts, .planning/phases/OF-09-project-manager-web-foundation/09-UI-SPEC.md, .planning/phases/OF-09-project-manager-web-foundation/09-PATTERNS.md</read_first>
  <action>Create `packages/web/src/components/projects/ProjectManagerPanel.tsx` and wire it into a new `project-manager` tab in the project detail page. Pass `projectId` and `enabled={activeTab === "project-manager"}` to the panel. Use the typed helpers from Plan 01 with TanStack Query keys scoped by project ID and resource. Render a compact goal summary, work item summary/list preview, and ledger marker preview. Render visible loading, empty goal, empty work item, not-found, and generic API error states with a retry/refresh command. Do not add full edit forms, status transition controls, evidence attachment controls, ledger filters, Gateway routes, or Next.js API routes.</action>
  <verify>
    <automated>rg -n "project-manager|ProjectManagerPanel|Project Manager|getProjectManagerGoal|listProjectManagerWorkItems|listProjectManagerLedger" packages/web/src/app/\\(dashboard\\)/projects/\\[id\\]/page.tsx packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Project detail includes a keyboard-reachable `Project Manager` tab.
    - Project-manager queries are disabled until the tab is active and `projectId` exists.
    - Loading, empty goal, empty work item, not-found, and generic API error states are visible in the tab panel.
    - The panel uses safe summary fields, counts, statuses, and timestamps only.
    - Unsupported Phase 10/11 actions are absent or disabled and do not appear executable.
    - No Gateway API behavior is implemented in Next.js.
  </acceptance_criteria>
  <done>The project detail workflow exposes a first-class Project Manager surface.</done>
</task>

<task type="auto">
  <name>Task 2: Add translated Project Manager copy</name>
  <files>packages/web/src/lib/i18n.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/app/(dashboard)/projects/[id]/page.tsx</files>
  <read_first>packages/web/src/lib/i18n.ts, .planning/phases/OF-09-project-manager-web-foundation/09-UI-SPEC.md, packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/app/(dashboard)/projects/[id]/page.tsx</read_first>
  <action>Add all new visible strings from `09-UI-SPEC.md` to `packages/web/src/lib/i18n.ts` for supported locales. Wire the tab label, section heading, refresh command, empty goal text, empty work item text, error text, not-found text, and disabled action hint through the existing translation helper instead of inline copy. Preserve existing project-page language patterns and avoid explanatory architecture text in the UI.</action>
  <verify>
    <automated>rg -n "projectManager|Project Manager|Refresh project manager|No project goal yet|No work items yet|Could not load project manager state|This action is planned for the next phase" packages/web/src/lib/i18n.ts packages/web/src/components/projects/ProjectManagerPanel.tsx packages/web/src/app/\\(dashboard\\)/projects/\\[id\\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - New visible Project Manager strings exist in `packages/web/src/lib/i18n.ts`.
    - The page and panel use translation keys for new visible labels and states.
    - Copy matches the Phase 9 UI design contract.
    - No explanatory product or architecture paragraphs are added to the UI.
  </acceptance_criteria>
  <done>Project Manager UI copy is localized through the existing i18n surface.</done>
</task>

<task type="auto">
  <name>Task 3: Add strict Project Manager E2E coverage</name>
  <files>packages/web/e2e/project-manager.spec.ts</files>
  <read_first>packages/web/e2e/copilot.spec.ts, packages/web/src/app/(dashboard)/projects/[id]/page.tsx, packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/lib/api.ts, .planning/phases/OF-09-project-manager-web-foundation/09-VALIDATION.md</read_first>
  <action>Create `packages/web/e2e/project-manager.spec.ts` with a narrow authenticated project detail flow. Mock the baseline project detail endpoints needed to open `/projects/:id`, then mock project-manager goal, work item, and ledger endpoints with exact paths. Add a strict fallback for unknown `/api/v1/*` routes that returns 404 and includes the unhandled route. Cover at least one populated state and one API error or not-found state. Use semantic controls or stable test IDs, not brittle ancestor text selectors.</action>
  <verify>
    <automated>pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - The E2E opens a project detail page and activates the Project Manager tab.
    - The populated state verifies goal, work item, and ledger summary rendering.
    - The error or not-found path verifies visible recovery/error text.
    - Unknown `/api/v1/*` routes fail through a 404 fallback.
    - Selectors use semantic roles, names, or stable test IDs.
  </acceptance_criteria>
  <done>Project Manager Web foundation has strict E2E coverage for the visible workflow and route contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/web vitest run src/lib/api.test.ts`
- [ ] `pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium`
- [ ] `git diff --check`
- [ ] `rg -n "project-manager|ProjectManagerPanel|projectManager|Unhandled mocked API route" packages/web/src/app/\\(dashboard\\)/projects/\\[id\\]/page.tsx packages/web/src/components/projects/ProjectManagerPanel.tsx packages/web/src/lib/i18n.ts packages/web/e2e/project-manager.spec.ts`
</verification>

<success_criteria>
- PMUX-01 is covered by a first-class Project Manager tab inside project detail.
- PMAPI-02 is covered by visible loading, empty, not-found, validation/API error, and retry states.
- Strict E2E mocks fail unknown project-manager API routes.
- The UI uses existing OpenForge control-plane styling and i18n without adding a global dashboard or backend authority changes.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-09-project-manager-web-foundation/09-02-SUMMARY.md`.
</output>
