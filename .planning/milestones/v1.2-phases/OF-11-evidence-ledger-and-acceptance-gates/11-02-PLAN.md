---
phase: 11-evidence-ledger-and-acceptance-gates
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - packages/web/src/components/projects/ProjectManagerPanel.tsx
  - packages/web/src/lib/i18n.ts
  - packages/web/e2e/project-manager.spec.ts
autonomous: true
requirements: [PMEV-02, PMEV-03, PMQA-01]
requirements_addressed: [PMEV-02, PMEV-03, PMQA-01]
user_setup: []
must_haves:
  truths:
    - "D-08: Current five-row ledger summary is replaced with a full in-tab ledger timeline area."
    - "D-09: Ledger loads 25 events by default and uses a manual `Load more` control instead of infinite scroll."
    - "D-10: Ledger filter groups are `All`, `Status changes`, `Evidence`, `Manual completion`, and `Blockers`."
    - "D-11: Filter mapping stays bounded using the existing Gateway event data and stable user-facing groups."
    - "D-12: Each ledger event shows event type, work item title or ID, status, evidence count, Feishu count, and timestamp."
    - "D-13: Ledger events do not expand raw event details or evidence reference lists."
    - "D-14: Blocker and manual-completion events use badges plus short explanatory copy without raw manual reason text."
    - "D-16: Error/regression coverage includes ledger load failure."
    - "D-17: Strict E2E mock coverage fails fast on Project Manager endpoint drift."
  artifacts:
    - "packages/web/src/components/projects/ProjectManagerPanel.tsx"
    - "packages/web/src/lib/i18n.ts"
    - "packages/web/e2e/project-manager.spec.ts"
  key_links:
    - "`11-PATTERNS.md` defines local ledger filter groups and safe row contents."
    - "`11-UI-SPEC.md` defines the timeline, filter, Load more, and error-state behavior."
---

<objective>
Replace the small ledger summary with a safe in-tab ledger timeline.

Purpose: PMEV-02 and PMEV-03 require users to review safe event markers and
distinguish status, evidence, manual completion, and blocker events without raw
sensitive details.
Output: The Project Manager tab shows a 25-event ledger timeline with compact
filters, manual Load more, scoped ledger error handling, and safe event rows.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-CONTEXT.md
@.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-RESEARCH.md
@.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-PATTERNS.md
@.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-VALIDATION.md
@.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-UI-SPEC.md
@docs/API.md
@packages/web/src/components/projects/ProjectManagerPanel.tsx
@packages/web/src/lib/api.ts
@packages/web/src/lib/i18n.ts
@packages/web/e2e/project-manager.spec.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Information disclosure | Ledger timeline | high | mitigate | Render only event type, work item title or ID, status, counts, timestamp, and static explanation copy; do not render event details, raw manual reason, or evidence ref lists from ledger rows. |
| T-11-05 | Integrity | Ledger filter mapping | medium | mitigate | Use stable user-facing filter groups mapped to existing event types; do not invent backend event semantics or broaden Gateway scope. |
| T-11-06 | Availability | Ledger query failure | medium | mitigate | Scope ledger load errors to the ledger area so goal/work item surfaces remain usable when ledger retrieval fails. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Convert ledger query state to 25-event timeline with Load more</name>
  <files>packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/lib/api.ts, docs/API.md, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-PATTERNS.md, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-UI-SPEC.md</read_first>
  <action>Add `LEDGER_PAGE_SIZE = 25`, `ledgerLimit` state initialized to 25, and a ledger query key that includes `{ limit: ledgerLimit }`. Change the query function to call `listProjectManagerLedger(projectId, { limit: ledgerLimit })`. Add a `Load more` handler that increases `ledgerLimit` by 25 and relies on the query key to refetch. Replace the five-row summary assumption with timeline data. Keep existing project-manager query invalidation broad enough that evidence/status mutations refresh ledger data.</action>
  <verify>
    <automated>rg -n "LEDGER_PAGE_SIZE|ledgerLimit|setLedgerLimit|limit: ledgerLimit|projectManagerLoadMoreLedger" packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Ledger default limit is 25.
    - `Load more` increments by 25.
    - Ledger query key includes the current limit.
    - No infinite scroll or intersection observer is introduced.
    - Evidence/status mutation invalidation still refreshes ledger queries.
  </acceptance_criteria>
  <done>Ledger retrieval is bounded by default and explicitly expanded by user action.</done>
</task>

<task type="auto">
  <name>Task 2: Add stable ledger filter groups</name>
  <files>packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/lib/api.ts, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-PATTERNS.md, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-UI-SPEC.md</read_first>
  <action>Add a `LedgerFilter` type with values `all`, `status_changes`, `evidence`, `manual_completion`, and `blockers`. Add local mapping: `status_changes` maps to `work_item_status_changed`; `evidence` maps to `evidence_attached`; `manual_completion` maps to `manual_completion_recorded`; `blockers` maps to `blocker_recorded` and `blocker_resolved`; `all` maps to every event. Filter the fetched ledger events locally with this mapping. Render compact filter controls labeled `All`, `Status changes`, `Evidence`, `Manual completion`, and `Blockers`. Reset no data on filter change. If a selected filter has no matching loaded events, render a filtered-empty state and keep `Load more` available.</action>
  <verify>
    <automated>rg -n "type LedgerFilter|status_changes|manual_completion|blocker_recorded|blocker_resolved|projectManagerLedgerFilter" packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The five user-facing filter labels exist.
    - `Blockers` includes both `blocker_recorded` and `blocker_resolved`.
    - Filtering is local or otherwise uses only existing Gateway capabilities.
    - Filter changes do not clear loaded goal or work item data.
    - Empty filtered state is visible.
  </acceptance_criteria>
  <done>Ledger timeline can be filtered by stable user-facing event groups.</done>
</task>

<task type="auto">
  <name>Task 3: Render safe ledger timeline rows and scoped errors</name>
  <files>packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/lib/api.ts, packages/web/src/lib/i18n.ts, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-CONTEXT.md, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-PATTERNS.md, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-UI-SPEC.md</read_first>
  <action>Replace `ProjectManagerLedgerCard` table rendering with timeline rows/cards. Pass `workItems`, filtered ledger events, active filter, ledger fetch/error state, `onFilterChange`, and `onLoadMore`. Resolve each event display name as the matching work item title from loaded work items or fall back to `event.workItemId`. Render event label/badge, work item title or ID, status badge or `-`, `event.evidenceRefCount`, `event.feishuRefCount`, and `formatTimestamp(event.createdAt)`. Add short static explanatory copy for `manual_completion_recorded`, `blocker_recorded`, and `blocker_resolved`. Do not render event details, raw manual reason, or evidence reference lists. Remove `ledgerQuery.error` from the global `firstError` that blanks the entire panel; instead render a ledger-area error with refresh control.</action>
  <verify>
    <automated>rg -n "ProjectManagerLedgerCard|projectManagerLedgerLoadFailed|projectManagerLedgerManualCompletionNote|projectManagerLedgerBlockerRecordedNote|workItemTitle|evidenceRefCount|feishuRefCount" packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Ledger failure no longer hides the goal and work item cards.
    - Timeline row/card shows event type, work item title or ID, status, evidence count, Feishu count, and timestamp.
    - Manual completion and blocker events have short static explanatory copy.
    - Raw manual reason text is not rendered.
    - Ledger event details and evidence reference lists are not rendered from the timeline.
  </acceptance_criteria>
  <done>Ledger review is safe, scannable, and failure-isolated.</done>
</task>

<task type="auto">
  <name>Task 4: Localize ledger timeline copy and cover ledger behavior in E2E</name>
  <files>packages/web/src/lib/i18n.ts, packages/web/e2e/project-manager.spec.ts</files>
  <read_first>packages/web/src/lib/i18n.ts, packages/web/e2e/project-manager.spec.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-VALIDATION.md</read_first>
  <action>Add ledger timeline keys to every existing dictionary. Required keys include `projects.projectManagerLedgerFilterAll`, `projects.projectManagerLedgerFilterStatusChanges`, `projects.projectManagerLedgerFilterEvidence`, `projects.projectManagerLedgerFilterManualCompletion`, `projects.projectManagerLedgerFilterBlockers`, `projects.projectManagerLoadMoreLedger`, `projects.projectManagerLedgerLoadFailed`, `projects.projectManagerLedgerFilteredEmptyTitle`, `projects.projectManagerLedgerFilteredEmptyBody`, `projects.projectManagerLedgerManualCompletionNote`, `projects.projectManagerLedgerBlockerRecordedNote`, and `projects.projectManagerLedgerBlockerResolvedNote`. Extend Playwright mock ledger handling so default ledger requests assert `limit=25`, Load more asserts the next limit, and the returned fixture includes `work_item_status_changed`, `evidence_attached`, `manual_completion_recorded`, `blocker_recorded`, and `blocker_resolved`. Add E2E assertions for filter labels, blocker/manual badges or notes, Load more behavior, and a ledger load failure that leaves goal/work item content visible.</action>
  <verify>
    <automated>pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - Ledger filter and timeline keys exist in Simplified Chinese, Traditional Chinese, and English dictionaries.
    - E2E asserts ledger default `limit=25`.
    - E2E asserts Load more grows the requested limit.
    - E2E proves blocker and manual-completion events are distinguishable.
    - E2E proves ledger load failure is scoped and goal/work item data remains visible.
    - Unknown `/api/v1/*` route fallback remains strict.
  </acceptance_criteria>
  <done>Ledger timeline copy and behavior are covered by strict E2E tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/web run typecheck`
- [ ] `pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium`
- [ ] `git diff --check`
- [ ] `rg -n "LEDGER_PAGE_SIZE|LedgerFilter|projectManagerLedgerFilter|projectManagerLoadMoreLedger|projectManagerLedgerLoadFailed" packages/web/src/components/projects/ProjectManagerPanel.tsx packages/web/src/lib/i18n.ts packages/web/e2e/project-manager.spec.ts`
</verification>

<success_criteria>
- PMEV-02 is covered by a safe ledger timeline with event markers, status, counts, and timestamps.
- PMEV-03 is covered by distinguishable status-change, evidence, manual-completion, and blocker event groups.
- Ledger review does not expose raw event details, raw manual reason, or evidence reference lists.
- Ledger load failure is recoverable and does not blank the full Project Manager tab.
- PMQA-01 has strict E2E coverage for ledger default limit, load-more behavior, filters, and scoped failure.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-02-SUMMARY.md`.
</output>

