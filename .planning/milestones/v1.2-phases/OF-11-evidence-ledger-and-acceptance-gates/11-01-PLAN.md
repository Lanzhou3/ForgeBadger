---
phase: 11-evidence-ledger-and-acceptance-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/projects/ProjectManagerPanel.tsx
  - packages/web/src/lib/i18n.ts
  - packages/web/src/lib/api.test.ts
  - packages/web/e2e/project-manager.spec.ts
autonomous: true
requirements: [PMEV-01, PMQA-01]
requirements_addressed: [PMEV-01, PMQA-01]
user_setup: []
must_haves:
  truths:
    - "D-01: Evidence attachment entry point is inside the existing work item detail Sheet."
    - "D-02: Work item table rows do not get direct evidence attachment buttons."
    - "D-03: One evidence attachment submission creates exactly one evidence reference."
    - "D-04: The attachment form exposes only `kind`, `label`, `ref`, and `path`."
    - "D-05: The attachment form does not expose raw evidence, terminal output, Feishu body, provider payload, transcript, secret, or note/body paste areas."
    - "D-06: Web blocks obvious sensitive or raw-content values in `ref` and `path`; Gateway remains final authority."
    - "D-07: Failed evidence attachment keeps the detail Sheet open and entered safe values recoverable."
    - "D-17: Typed client and strict E2E mock coverage fail fast on Project Manager endpoint drift."
  artifacts:
    - "packages/web/src/components/projects/ProjectManagerPanel.tsx"
    - "packages/web/src/lib/i18n.ts"
    - "packages/web/src/lib/api.test.ts"
    - "packages/web/e2e/project-manager.spec.ts"
  key_links:
    - "`docs/API.md` defines allowed evidence reference fields and Gateway authority."
    - "`11-RESEARCH.md` defines the Web-side sensitive-content guard shape."
    - "`11-UI-SPEC.md` defines evidence form placement, copy, and recoverable failure behavior."
---

<objective>
Add bounded post-creation evidence attachment from the work item detail Sheet.

Purpose: PMEV-01 requires a user to attach evidence references using only the
approved Phase 11 fields, while PMQA-01 requires the typed client and strict E2E
route contract to prove the path.
Output: The selected work item detail Sheet exposes a one-reference evidence
form for `kind`, `label`, `ref`, and `path`; blocks obvious unsafe values before
submit; calls the existing Gateway client; refreshes work items and ledger on
success; and keeps safe draft values recoverable on failure.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-CONTEXT.md
@.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-RESEARCH.md
@.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-PATTERNS.md
@.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-VALIDATION.md
@.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-UI-SPEC.md
@docs/API.md
@packages/web/src/components/projects/ProjectManagerPanel.tsx
@packages/web/src/lib/api.ts
@packages/web/src/lib/i18n.ts
@packages/web/src/lib/api.test.ts
@packages/web/e2e/project-manager.spec.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Information disclosure | Evidence attach form | high | mitigate | Expose only `kind`, `label`, `ref`, and `path`; do not add raw evidence body, terminal transcript, Feishu body, provider payload, or note paste fields. |
| T-11-02 | Information disclosure | Web-side evidence validation | high | mitigate | Add a local guard for obvious API keys, JWTs, private keys, attach tokens, raw terminal text, and provider-payload-like blobs in `ref` and `path`; still submit only to Gateway authority. |
| T-11-03 | Availability | Evidence mutation failure | medium | mitigate | Keep the detail Sheet open, preserve safe draft values, show a local error, and do not clear existing work item or ledger state on mutation failure. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add evidence attach draft, safe guard, and mutation</name>
  <files>packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/lib/api.ts, docs/API.md, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-CONTEXT.md, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-RESEARCH.md, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-PATTERNS.md, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-UI-SPEC.md</read_first>
  <action>Import and use `attachProjectManagerWorkItemEvidence`. Add an `EvidenceDraft` with `kind`, `label`, `ref`, and `path`, evidence form error state, and an evidence mutation in `ProjectManagerPanel`. Add `createSingleEvidenceReference(draft)` that trims the four fields, drops empty optional values, requires a non-empty `kind`, and requires at least one of `ref` or `path`. Add `validateEvidenceReferenceInput(draft)` that blocks unsafe values in `ref` and `path` matching obvious API key prefixes, `Bearer`, JWT-like tokens, private-key headers, `OPENFORGE_ATTACH_TOKEN=`, key-value secret names, raw terminal markers, command-output markers, control/newline characters, and provider-payload-like JSON. Submit exactly `{ evidenceRefs: [reference] }` with one reference. On success, clear the draft and error, invalidate `["project-manager", projectId, "work-items"]` and `["project-manager", projectId, "ledger"]`. On failure, keep the selected Sheet open, preserve safe draft fields, show `projects.projectManagerEvidenceAttachError`, and do not clear existing work item or ledger data. Keep new hooks before conditional returns if moving hook placement is needed.</action>
  <verify>
    <automated>rg -n "attachProjectManagerWorkItemEvidence|EvidenceDraft|createSingleEvidenceReference|validateEvidenceReferenceInput|projectManagerEvidenceAttachError|OPENFORGE_ATTACH_TOKEN|Bearer|private key|evidenceRefs: \\[reference\\]" packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ProjectManagerPanel` imports `attachProjectManagerWorkItemEvidence`.
    - Evidence attach state contains only `kind`, `label`, `ref`, and `path`.
    - Submit body is exactly one evidence reference in `evidenceRefs`.
    - Unsafe `ref` or `path` values are blocked before the API call.
    - The guard includes API key, bearer token, JWT, private key, attach token, raw terminal, command output, and provider payload patterns.
    - Success invalidates Project Manager work item and ledger query keys.
    - Failure keeps the Sheet open and safe field values visible.
    - No Next.js API route or Gateway code is added.
  </acceptance_criteria>
  <done>Evidence attachment is wired through the existing Gateway client with local safety guardrails.</done>
</task>

<task type="auto">
  <name>Task 2: Render the evidence form inside the work item detail Sheet</name>
  <files>packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/components/ui/input.tsx, packages/web/src/components/ui/label.tsx, packages/web/src/components/ui/button.tsx, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-UI-SPEC.md</read_first>
  <action>Extend `ProjectManagerWorkItemDetailSheet` props with the evidence draft, local error, pending state, draft update callback, and submit callback. Render a compact evidence attachment section after the existing evidence reference read-only list. The section must have labels and inputs for `kind`, `label`, `ref`, and `path` only. Add short helper text that evidence references are pointers and raw terminal transcripts, Feishu bodies, provider payloads, and secrets should not be pasted. Disable inputs and submit while the mutation is pending. Do not add attach controls to `ProjectManagerWorkItemsCard` table rows.</action>
  <verify>
    <automated>rg -n "projectManagerAttachEvidence|projectManagerEvidenceReferenceHint|projectManagerEvidenceUnsafeValue|project-manager-evidence-kind|project-manager-evidence-path" packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Evidence attach controls appear only in `ProjectManagerWorkItemDetailSheet`.
    - Work item table rows do not contain a direct evidence attach button.
    - Form controls are labeled for `kind`, `label`, `ref`, and `path`.
    - Helper copy warns against raw terminal transcripts, Feishu bodies, provider payloads, and secrets.
    - The section does not use `Textarea`.
    - Pending mutation disables form controls and submit button.
  </acceptance_criteria>
  <done>The work item detail Sheet has a bounded, accessible evidence attachment form.</done>
</task>

<task type="auto">
  <name>Task 3: Add localized evidence attachment copy</name>
  <files>packages/web/src/lib/i18n.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/lib/i18n.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-UI-SPEC.md</read_first>
  <action>Add evidence attachment keys to all existing dictionaries in `packages/web/src/lib/i18n.ts`. Required keys: `projects.projectManagerAttachEvidence`, `projects.projectManagerEvidenceReferenceHint`, `projects.projectManagerEvidenceKindRequired`, `projects.projectManagerEvidenceReferenceRequired`, `projects.projectManagerEvidenceUnsafeValue`, `projects.projectManagerEvidenceAttachError`, and `projects.projectManagerEvidenceAttachSuccess` if success copy is shown. Wire all new visible labels, helpers, errors, and actions through `t(...)`; do not add inline user-facing strings in `ProjectManagerPanel`.</action>
  <verify>
    <automated>rg -n "projectManagerAttachEvidence|projectManagerEvidenceReferenceHint|projectManagerEvidenceKindRequired|projectManagerEvidenceReferenceRequired|projectManagerEvidenceUnsafeValue|projectManagerEvidenceAttachError" packages/web/src/lib/i18n.ts packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Evidence attachment keys exist in Simplified Chinese, Traditional Chinese, and English dictionaries.
    - All new evidence attachment UI copy uses translation keys.
    - Copy says evidence references are pointers and does not invite raw content.
  </acceptance_criteria>
  <done>Evidence attachment UI copy is localized.</done>
</task>

<task type="auto">
  <name>Task 4: Tighten typed client and E2E coverage for evidence attachment</name>
  <files>packages/web/src/lib/api.test.ts, packages/web/e2e/project-manager.spec.ts</files>
  <read_first>packages/web/src/lib/api.test.ts, packages/web/e2e/project-manager.spec.ts, packages/web/src/lib/api.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx, .planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-VALIDATION.md</read_first>
  <action>Extend the existing project-manager API test so `attachProjectManagerWorkItemEvidence("project/1", "work/item-1", ...)` asserts the encoded evidence URL, `POST` method, and a body containing exactly one `evidenceRefs` item with Phase 11 UI fields `kind`, `label`, `ref`, and `path`. Extend the strict Playwright mock with `POST /api/v1/projects/${PROJECT_ID}/project-manager/work-items/:workItemId/evidence` and state updates for evidence count/reference list and an `evidence_attached` ledger event. Add one E2E path that opens a work item detail Sheet, fills evidence `kind`, `label`, `ref`, `path`, submits, sees the evidence count/reference update, and asserts no unhandled API routes. Add one E2E failure path where evidence attach returns 500, the Sheet stays open, the safe field values remain, and the local error is visible.</action>
  <verify>
    <automated>pnpm --dir packages/web exec vitest run src/lib/api.test.ts</automated>
    <automated>pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - API test asserts evidence attach URL includes encoded project and work item IDs.
    - API test asserts evidence attach body has one reference with `kind`, `label`, `ref`, and `path`.
    - E2E mock handles evidence attach explicitly and unknown `/api/v1/*` routes still return 404.
    - E2E success path proves evidence count/reference update after submit.
    - E2E failure path proves Sheet and safe draft values are recoverable.
  </acceptance_criteria>
  <done>Evidence attachment has typed client and strict E2E coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/web run typecheck`
- [ ] `pnpm --dir packages/web exec vitest run src/lib/api.test.ts`
- [ ] `pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium`
- [ ] `git diff --check`
- [ ] `rg -n "attachProjectManagerWorkItemEvidence|createSingleEvidenceReference|validateEvidenceReferenceInput|projectManagerAttachEvidence" packages/web/src/components/projects/ProjectManagerPanel.tsx packages/web/src/lib/i18n.ts packages/web/src/lib/api.test.ts packages/web/e2e/project-manager.spec.ts`
</verification>

<success_criteria>
- PMEV-01 is covered by a bounded evidence attachment form in the work item detail Sheet.
- Evidence attachment submits exactly one structured reference through Gateway.
- Unsafe raw or secret-like `ref`/`path` values are blocked locally before submit.
- Mutation failure is recoverable and does not blank Project Manager data.
- PMQA-01 has typed client and strict E2E route-contract coverage for evidence attachment.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-11-evidence-ledger-and-acceptance-gates/11-01-SUMMARY.md`.
</output>

