---
phase: 10-goal-and-work-item-operations
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - packages/web/src/components/projects/ProjectManagerPanel.tsx
  - packages/web/src/lib/i18n.ts
  - packages/web/e2e/project-manager.spec.ts
autonomous: true
requirements: [PMUX-05]
requirements_addressed: [PMUX-05]
user_setup: []
must_haves:
  truths:
    - "D-11: Status movement is explicit next actions derived from the Gateway transition table."
    - "D-12: `done` and `cancelled` are terminal in the Web experience."
    - "D-13: Evidence-free `done` requires a non-empty manual completion reason before submission."
    - "D-14: Status mutations refetch relevant Project Manager queries."
    - "D-15: Focused Web tests cover status transition success/error and done guard with manual reason."
    - "D-16: Strict Playwright coverage keeps unknown `/api/v1/*` routes failing fast while proving status movement and done transition."
  artifacts:
    - "packages/web/src/components/projects/ProjectManagerPanel.tsx"
    - "packages/web/src/lib/i18n.ts"
    - "packages/web/e2e/project-manager.spec.ts"
  key_links:
    - "`docs/API.md` is the source of truth for allowed transitions and done guard."
    - "`10-PATTERNS.md` lists the exact transition map to copy."
    - "`10-UI-SPEC.md` defines status action and manual reason UI behavior."
---

<objective>
Implement work item status movement and evidence-free done guard.

Purpose: PMUX-05 requires users to move work items through allowed status
transitions and receive a clear blocked state when completion lacks evidence or
a manual completion reason.
Output: Work item detail/list actions show only allowed next transitions,
submit status mutations through Gateway, require manual completion reason for
evidence-free `done`, surface mutation errors, and refresh persisted state.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-CONTEXT.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-RESEARCH.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-PATTERNS.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-VALIDATION.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md
@docs/API.md
@packages/web/src/components/projects/ProjectManagerPanel.tsx
@packages/web/src/lib/api.ts
@packages/web/src/lib/i18n.ts
@packages/web/e2e/project-manager.spec.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-07 | Integrity | Web status transition map | high | mitigate | Keep a single local map matching `docs/API.md`; render only allowed next actions while Gateway remains authoritative. |
| T-10-08 | Integrity | Done guard | high | mitigate | Require non-empty `manualCompletionReason` before evidence-free `done` submission and still surface Gateway rejection if it occurs. |
| T-10-09 | Information disclosure | Manual completion reason | medium | mitigate | Treat the reason as a bounded completion guard; do not show it as raw ledger detail or encourage secrets/transcripts. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add allowed transition actions</name>
  <files>packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/components/ui/dropdown-menu.tsx, packages/web/src/lib/api.ts, docs/API.md, .planning/phases/OF-10-goal-and-work-item-operations/10-PATTERNS.md, .planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md</read_first>
  <action>Add a local `PROJECT_MANAGER_STATUS_TRANSITIONS` map in `ProjectManagerPanel` matching `docs/API.md`: `todo` to `in_progress`, `blocked`, `cancelled`; `in_progress` to `blocked`, `ready_for_review`, `done`, `cancelled`; `blocked` to `todo`, `in_progress`, `cancelled`; `ready_for_review` to `in_progress`, `done`, `cancelled`; `done` to empty; `cancelled` to empty. Render transition actions from this map in the work item table/detail using existing Button or DropdownMenu primitives. Do not render actions for terminal statuses. Use existing status labels for action text.</action>
  <verify>
    <automated>rg -n "PROJECT_MANAGER_STATUS_TRANSITIONS|todo:|in_progress:|ready_for_review:|cancelled:|projectManagerChangeStatus" packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - One local transition map exists and matches `docs/API.md` exactly.
    - `done` and `cancelled` render no next status actions.
    - Visible actions use translated status labels.
    - The UI does not render a free-form all-status dropdown for status movement.
  </acceptance_criteria>
  <done>Users see only documented next status actions for each work item.</done>
</task>

<task type="auto">
  <name>Task 2: Implement status mutation and done manual reason guard</name>
  <files>packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/components/ui/dialog.tsx, packages/web/src/components/ui/textarea.tsx, packages/web/src/lib/api.ts, docs/API.md, .planning/phases/OF-10-goal-and-work-item-operations/10-CONTEXT.md, .planning/phases/OF-10-goal-and-work-item-operations/10-RESEARCH.md, .planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md</read_first>
  <action>Import/use `updateProjectManagerWorkItemStatus`. Add status mutation state and error rendering. When a non-`done` transition is selected, submit `{ status: nextStatus }`. When `done` is selected and the item has `evidenceRefCount > 0`, submit `{ status: "done" }`. When `done` is selected and `evidenceRefCount === 0`, open a manual completion reason dialog, focus the reason field, require non-empty trimmed text, and submit `{ status: "done", manualCompletionReason }`. Disable controls while pending. On success, close any dialog, clear reason state, and invalidate/refetch work item list, selected detail if applicable, and ledger summary. On failure, keep the item/detail visible and render a status-specific error.</action>
  <verify>
    <automated>rg -n "updateProjectManagerWorkItemStatus|manualCompletionReason|projectManagerDoneReason|projectManagerStatusMutationError|evidenceRefCount" packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Non-`done` status actions call `updateProjectManagerWorkItemStatus` with `{ status }`.
    - `done` with evidence references submits without manual reason.
    - `done` without evidence references requires non-empty manual completion reason before mutation.
    - Empty/whitespace manual reason does not submit.
    - Status mutation errors are visible and recoverable.
    - Successful status mutation refreshes list/detail/ledger state.
  </acceptance_criteria>
  <done>Status changes and evidence-free completion guard are implemented through Gateway mutations.</done>
</task>

<task type="auto">
  <name>Task 3: Localize status movement and done guard copy</name>
  <files>packages/web/src/lib/i18n.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/lib/i18n.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx, .planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md</read_first>
  <action>Add status movement keys to all existing dictionaries in `packages/web/src/lib/i18n.ts`. Required keys include `projects.projectManagerChangeStatus`, `projects.projectManagerDoneReasonTitle`, `projects.projectManagerDoneReasonBody`, `projects.projectManagerDoneReasonLabel`, `projects.projectManagerDoneReasonRequired`, `projects.projectManagerConfirmStatusChange`, and `projects.projectManagerStatusMutationError`. Wire all visible status movement labels, dialog title/body/label, validation text, and mutation errors through `t(...)`.</action>
  <verify>
    <automated>rg -n "projectManagerChangeStatus|projectManagerDoneReasonTitle|projectManagerDoneReasonBody|projectManagerDoneReasonRequired|projectManagerStatusMutationError" packages/web/src/lib/i18n.ts packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Status movement keys exist in Simplified Chinese, Traditional Chinese, and English dictionaries.
    - `ProjectManagerPanel` uses translation keys for status action and done guard copy.
    - Manual completion reason copy is short and does not invite secrets, terminal output, or transcripts.
  </acceptance_criteria>
  <done>Status transition and done guard copy is localized.</done>
</task>

<task type="auto">
  <name>Task 4: Cover status transitions and done guard in E2E</name>
  <files>packages/web/e2e/project-manager.spec.ts</files>
  <read_first>packages/web/e2e/project-manager.spec.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/lib/api.ts, docs/API.md, .planning/phases/OF-10-goal-and-work-item-operations/10-VALIDATION.md</read_first>
  <action>Extend `project-manager.spec.ts` mocks for `PATCH /api/v1/projects/${PROJECT_ID}/project-manager/work-items/:workItemId/status`. Add E2E coverage that opens a work item with status `in_progress`, sees allowed actions including `ready_for_review`, performs a status change, and sees refreshed status. Add coverage for an evidence-free `done` attempt: selecting `done` opens the manual completion reason prompt, blank reason does not submit, non-empty reason submits `manualCompletionReason`, and the mock returns updated status `done`. Assert no unhandled API routes were recorded.</action>
  <verify>
    <automated>pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - E2E proves a documented status transition submits the expected PATCH body.
    - E2E proves evidence-free `done` opens manual reason prompt.
    - E2E proves blank manual reason does not submit.
    - E2E proves non-empty manual reason submits as `manualCompletionReason`.
    - Updated status is visible after mocked success.
    - Strict unknown `/api/v1/*` fallback remains active.
  </acceptance_criteria>
  <done>Status transition and done guard behavior has strict E2E coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/web run typecheck`
- [ ] `pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium`
- [ ] `git diff --check`
- [ ] `gsd-sdk query verify.schema-drift 10`
- [ ] `rg -n "PROJECT_MANAGER_STATUS_TRANSITIONS|updateProjectManagerWorkItemStatus|manualCompletionReason|projectManagerDoneReason" packages/web/src/components/projects/ProjectManagerPanel.tsx packages/web/src/lib/i18n.ts packages/web/e2e/project-manager.spec.ts`
</verification>

<success_criteria>
- PMUX-05 is covered by documented status movement and done guard behavior.
- The Web UI does not present invalid status moves as normal actions.
- Evidence-free completion cannot submit without manual completion reason.
- Gateway remains the final authority for status mutations and errors.
- Done/cancelled terminal states are visibly non-actionable.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-10-goal-and-work-item-operations/10-03-SUMMARY.md`.
</output>
