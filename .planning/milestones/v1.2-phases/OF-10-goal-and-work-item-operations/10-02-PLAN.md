---
phase: 10-goal-and-work-item-operations
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - packages/web/src/components/projects/ProjectManagerPanel.tsx
  - packages/web/src/lib/i18n.ts
  - packages/web/src/lib/api.test.ts
  - packages/web/e2e/project-manager.spec.ts
autonomous: true
requirements: [PMUX-03, PMUX-04]
requirements_addressed: [PMUX-03, PMUX-04]
user_setup: []
must_haves:
  truths:
    - "D-05: Work item listing uses bounded Gateway status filtering."
    - "D-06: Work item detail stays inside project context through a sheet or same-tab detail panel."
    - "D-07: Phase 10 inspection shows safe fields and counts, not a full evidence attachment workflow."
    - "D-08: Create work item collects title, description, priority, acceptance criteria, optional status, and defaults to `todo`."
    - "D-09: Optional initial refs are bounded `evidenceRefs` / `feishuRefs` using approved fields from `docs/API.md`."
    - "D-10: Post-creation evidence attachment remains out of Phase 10."
    - "D-15: Focused Web tests cover create work item success/error, status filter query shape, and panel behavior."
    - "D-16: Strict Playwright coverage keeps unknown `/api/v1/*` routes failing fast while proving work item creation, filtering, and inspection."
  artifacts:
    - "packages/web/src/components/projects/ProjectManagerPanel.tsx"
    - "packages/web/src/lib/i18n.ts"
    - "packages/web/src/lib/api.test.ts"
    - "packages/web/e2e/project-manager.spec.ts"
  key_links:
    - "`10-PATTERNS.md` defines bounded query keys, text-list normalization, and reference fields."
    - "`10-UI-SPEC.md` defines work item list/detail/create interaction contracts."
---

<objective>
Implement work item filtering, inspection, and creation in the Project Manager tab.

Purpose: PMUX-03 and PMUX-04 require users to list, filter, inspect, and create
project-manager work items without leaving the project page.
Output: The work item section supports bounded status filtering, in-context
detail inspection, a compact create flow, bounded initial references, i18n, and
strict route-contract coverage.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-CONTEXT.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-RESEARCH.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-PATTERNS.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-VALIDATION.md
@.planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md
@docs/API.md
@packages/web/src/components/projects/ProjectManagerPanel.tsx
@packages/web/src/lib/api.ts
@packages/web/src/lib/api.test.ts
@packages/web/src/lib/i18n.ts
@packages/web/e2e/project-manager.spec.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-04 | Integrity | Work item filters | high | mitigate | Use Gateway `status` and bounded `limit` query params instead of unbounded client-side filtering. |
| T-10-05 | Information disclosure | Work item detail and references | high | mitigate | Display safe fields, counts, and approved reference identifiers only; do not expose raw evidence blobs or transcript-like inputs. |
| T-10-06 | Integrity | Work item creation | high | mitigate | Submit only Gateway-supported create fields and keep optional initial refs bounded to approved evidence reference keys. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add status-filtered work item listing and in-context detail</name>
  <files>packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/components/ui/sheet.tsx, packages/web/src/lib/api.ts, docs/API.md, .planning/phases/OF-10-goal-and-work-item-operations/10-PATTERNS.md, .planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md</read_first>
  <action>Replace the fixed work item summary query with a bounded status-filtered query. Add local filter state supporting `all`, `todo`, `in_progress`, `blocked`, `ready_for_review`, `done`, and `cancelled`. Use `listProjectManagerWorkItems(projectId, { status, limit: 50 })` when a bounded status is selected and `listProjectManagerWorkItems(projectId, { limit: 50 })` for `all`. Update the query key to include status and limit. Add an in-context detail sheet or panel opened by a row action. Detail content must show title, description, status, priority, acceptance criteria, evidence count, Feishu ref count, created/updated timestamps, and safe reference identifiers if already returned. Do not add post-creation evidence attachment controls.</action>
  <verify>
    <automated>rg -n "projectManagerStatusFilter|listProjectManagerWorkItems\\(projectId|limit: 50|Sheet|projectManagerViewDetails|evidenceRefCount|feishuRefCount" packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Work item query key includes project ID, selected status/all, and bounded limit.
    - Selecting a bounded status sends the Gateway `status` query param.
    - Selecting all statuses omits the Gateway `status` query param.
    - Empty state distinguishes selected-filter emptiness from no work items overall.
    - Work item detail opens without route navigation.
    - Detail shows safe fields and counts only.
    - No evidence attachment control is introduced.
  </acceptance_criteria>
  <done>Users can filter and inspect work items inside the Project Manager tab.</done>
</task>

<task type="auto">
  <name>Task 2: Add compact create work item flow with bounded initial refs</name>
  <files>packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/components/ui/dialog.tsx, packages/web/src/components/ui/textarea.tsx, packages/web/src/lib/api.ts, docs/API.md, .planning/phases/OF-10-goal-and-work-item-operations/10-CONTEXT.md, .planning/phases/OF-10-goal-and-work-item-operations/10-RESEARCH.md, .planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md</read_first>
  <action>Add a create work item dialog or sheet in `ProjectManagerPanel`. Import/use `createProjectManagerWorkItem`. Form fields: `title` required, optional `description`, numeric `priority`, `status` defaulting to `todo`, and newline-separated `acceptanceCriteria`. Add one optional initial evidence reference group with fields `kind`, `label`, `ref`, and `path`; include it in `evidenceRefs` only when at least one identifier field (`ref` or `path`) is non-empty after trim. Add one optional initial Feishu reference group with fields `kind`, `label`, `ref`, and `feishuMessageId`; include it in `feishuRefs` only when at least one identifier field (`ref` or `feishuMessageId`) is non-empty after trim. Do not add raw multiline evidence, Feishu body, provider payload, transcript, or terminal output inputs. On submit, call `createProjectManagerWorkItem(projectId, input)`, disable controls while pending, render a create-specific mutation error, close/reset on success, and refetch/invalidate work item plus ledger queries.</action>
  <verify>
    <automated>rg -n "createProjectManagerWorkItem|projectManagerCreateWorkItem|projectManagerWorkItemTitle|evidenceRefs|feishuRefs|parseProjectManagerTextList" packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Create action is visible in the work item operations area.
    - Title is required before submitting.
    - Form submits title, description, priority, status, acceptance criteria, and optional bounded initial refs.
    - Acceptance criteria normalize by newline trim/drop-empty.
    - Initial evidence ref fields are limited to `kind`, `label`, `ref`, and `path`.
    - Initial Feishu ref fields are limited to `kind`, `label`, `ref`, and `feishuMessageId`.
    - Initial ref inputs do not accept raw terminal output, Feishu message bodies, provider payloads, or transcripts.
    - Successful create refreshes visible work item data.
    - Mutation error is visible and keeps the dialog/form recoverable.
  </acceptance_criteria>
  <done>Users can create bounded project-manager work items from the project tab.</done>
</task>

<task type="auto">
  <name>Task 3: Localize work item operations copy</name>
  <files>packages/web/src/lib/i18n.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx</files>
  <read_first>packages/web/src/lib/i18n.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx, .planning/phases/OF-10-goal-and-work-item-operations/10-UI-SPEC.md</read_first>
  <action>Add work item operation keys to all existing dictionaries in `packages/web/src/lib/i18n.ts`. Required keys include `projects.projectManagerCreateWorkItem`, `projects.projectManagerViewDetails`, `projects.projectManagerFilterByStatus`, `projects.projectManagerAllStatuses`, `projects.projectManagerWorkItemTitle`, `projects.projectManagerWorkItemDescription`, `projects.projectManagerInitialEvidenceRefs`, `projects.projectManagerInitialFeishuRefs`, `projects.projectManagerCreateWorkItemError`, `projects.projectManagerFilterEmptyTitle`, and `projects.projectManagerFilterEmptyBody`. Wire all visible labels, helpers, empty states, and errors through `t(...)`.</action>
  <verify>
    <automated>rg -n "projectManagerCreateWorkItem|projectManagerViewDetails|projectManagerFilterByStatus|projectManagerAllStatuses|projectManagerInitialEvidenceRefs|projectManagerFilterEmptyTitle" packages/web/src/lib/i18n.ts packages/web/src/components/projects/ProjectManagerPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Work item operation keys exist in Simplified Chinese, Traditional Chinese, and English dictionaries.
    - `ProjectManagerPanel` uses translation keys for all new work item labels, helper text, empty states, and errors.
    - No explanatory GSD or architecture prose is added to the visible UI.
  </acceptance_criteria>
  <done>Work item operation copy is localized through the existing i18n surface.</done>
</task>

<task type="auto">
  <name>Task 4: Extend API and E2E coverage for filter/detail/create</name>
  <files>packages/web/src/lib/api.test.ts, packages/web/e2e/project-manager.spec.ts</files>
  <read_first>packages/web/src/lib/api.test.ts, packages/web/e2e/project-manager.spec.ts, packages/web/src/components/projects/ProjectManagerPanel.tsx, packages/web/src/lib/api.ts, docs/API.md, .planning/phases/OF-10-goal-and-work-item-operations/10-VALIDATION.md</read_first>
  <action>Add focused `api.test.ts` assertions for `listProjectManagerWorkItems(projectId, { status: "blocked", limit: 50 })` and `createProjectManagerWorkItem` with title, description, priority, status, acceptanceCriteria, evidenceRefs, and feishuRefs. Extend `project-manager.spec.ts` mocks for filtered `GET /work-items`, `POST /work-items`, and `GET /work-items/:workItemId` if the implemented detail flow fetches detail separately. Add E2E coverage that filters by status, opens detail, creates a work item with bounded fields, sees it in the list, and records no unhandled API routes.</action>
  <verify>
    <automated>pnpm --dir packages/web exec vitest run src/lib/api.test.ts</automated>
    <automated>pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - API tests assert work item status filter query shape with `status` and `limit`.
    - API tests assert create payload includes bounded work item fields and reference arrays.
    - E2E covers status filtering.
    - E2E covers in-context work item detail.
    - E2E covers successful work item creation.
    - Strict unknown `/api/v1/*` fallback remains active.
  </acceptance_criteria>
  <done>Work item list/detail/create behavior has focused route-contract coverage.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir packages/web run typecheck`
- [ ] `pnpm --dir packages/web exec vitest run src/lib/api.test.ts`
- [ ] `pnpm --dir packages/web exec playwright test e2e/project-manager.spec.ts --project=chromium`
- [ ] `git diff --check`
- [ ] `rg -n "projectManagerCreateWorkItem|projectManagerFilterByStatus|createProjectManagerWorkItem|evidenceRefs|feishuRefs" packages/web/src/components/projects/ProjectManagerPanel.tsx packages/web/src/lib/i18n.ts packages/web/src/lib/api.test.ts packages/web/e2e/project-manager.spec.ts`
</verification>

<success_criteria>
- PMUX-03 is covered by bounded status filtering and in-context work item inspection.
- PMUX-04 is covered by compact work item creation with title, description, priority, acceptance criteria, status, and optional bounded initial references.
- Raw evidence blobs, post-creation evidence attachment, Gateway model changes, and global dashboard routes remain absent.
- Strict tests fail on endpoint contract drift.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-10-goal-and-work-item-operations/10-02-SUMMARY.md`.
</output>
