---
phase: 07-feishu-live-callback-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/prepare-feishu-public-webhook.ts
  - scripts/prepare-feishu-public-webhook.test.ts
  - package.json
  - docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md
autonomous: false
requirements: [FEI-01]
requirements_addressed: [FEI-01]
user_setup:
  - "Required for FEI-01 pass: public HTTPS URL that routes to Gateway `/api/v1/integrations/feishu/webhook/:publicId`."
  - "Required for FEI-01 pass: Feishu developer-console access for event subscription URL verification."
  - "Required for live event pass beyond URL verification: allowed Feishu chat id and mapped Feishu open_id."
must_haves:
  truths:
    - "D-01: `lark-cli event consume` is auxiliary preflight and cannot satisfy FEI-01 by itself."
    - "D-02: FEI-01 `Pass` requires a real Feishu developer-console HTTP callback attempt against `POST /api/v1/integrations/feishu/webhook/:publicId`."
    - "D-03: Real callback evidence records public URL shape, Gateway environment, route/public id, attempted Feishu callback action, result, sanitized logs/audit evidence, owner, and rerun steps."
    - "D-04: URL verification proves reachability and token match only; it does not prove message-event policy, tenant mapping, replay, rate, or approval boundaries."
    - "D-05: Real `im.message.receive_v1` evidence is preferred when available; otherwise preserve the live-event caveat and use automated regression coverage."
    - "D-16: Evidence reports use explicit `Pass`, `Caveat`, and `Blocked` states."
    - "D-17: CLI version drift is advisory when auth and doctor pass."
    - "D-18: `git diff --check` and targeted secret scan run before committing Phase 7 evidence docs."
  artifacts:
    - "docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md"
    - "scripts/prepare-feishu-public-webhook.ts"
    - "scripts/prepare-feishu-public-webhook.test.ts"
  key_links:
    - "The Feishu evidence report separates CLI preflight, setup helper, real console callback, and optional live message-event evidence."
    - "A simulated signed Gateway request is never labeled as FEI-01 pass unless a real console callback also occurred."
---

<objective>
Prepare and run the Feishu developer-console callback verification path, or record the exact live-app blocker.

Purpose: FEI-01 needs a reproducible route from Feishu developer console to OpenForge's existing public webhook route, without leaking webhook secrets or confusing CLI long-connection evidence with product callback evidence.
Output: a safe setup helper if needed, CLI preflight evidence, and `docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md` with real callback status as `Pass`, `Caveat`, or `Blocked`.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/OF-07-feishu-live-callback-readiness/07-CONTEXT.md
@.planning/phases/OF-07-feishu-live-callback-readiness/07-RESEARCH.md
@.planning/phases/OF-07-feishu-live-callback-readiness/07-PATTERNS.md
@.planning/phases/OF-07-feishu-live-callback-readiness/07-VALIDATION.md
@docs/API.md
@docs/reports/v1.1-beta-evidence-burn-down-2026-05-21.md
@packages/gateway/src/routes/integrations-feishu.ts
@packages/gateway/src/db/repositories/feishu-integration-repository.ts
@packages/gateway/src/services/integrations/feishu-cli.ts
@packages/gateway/test/feishu-integration.test.ts
</context>

<threat_model>
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Integrity | FEI-01 evidence classification | critical | mitigate | Label CLI event-consumer and local signed-route tests as auxiliary/simulated only. Use `Pass` for FEI-01 only after real Feishu developer-console HTTP callback evidence. |
| T-07-02 | Information disclosure | Callback setup and evidence | critical | mitigate | Read verification token and event encrypt key from environment variables, store encrypted, and output only public id, callback path, counts, statuses, and redacted summaries. Do not print raw secrets, signatures, raw request bodies, JWTs, or private message text. |
| T-07-03 | Availability | Public webhook live exposure | high | mitigate | If public HTTPS URL, console access, or callback configuration is missing, record `Caveat` or `Blocked` with owner and rerun path instead of forcing an unsafe workaround. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add a safe public webhook setup helper</name>
  <files>scripts/prepare-feishu-public-webhook.ts, scripts/prepare-feishu-public-webhook.test.ts, package.json</files>
  <read_first>packages/gateway/src/config/env.ts, packages/gateway/src/db/client.ts, packages/gateway/src/db/repositories/user-repository.ts, packages/gateway/src/db/repositories/feishu-integration-repository.ts, scripts/smoke-copilot-provider.ts, scripts/smoke-copilot-provider.test.ts, package.json</read_first>
  <action>Add `scripts/prepare-feishu-public-webhook.ts` and `scripts/prepare-feishu-public-webhook.test.ts` only if there is no safer existing command for storing Feishu public webhook config. The script must read `OPENFORGE_DB_PATH`, `OPENFORGE_MASTER_KEY`, `OPENFORGE_FEISHU_OPENFORGE_USER_ID` or `OPENFORGE_FEISHU_OPENFORGE_USER_EMAIL`, `OPENFORGE_FEISHU_PUBLIC_WEBHOOK_ID`, `OPENFORGE_FEISHU_PUBLIC_WEBHOOK_ENABLED`, `OPENFORGE_FEISHU_WEBHOOK_VERIFICATION_TOKEN`, `OPENFORGE_FEISHU_WEBHOOK_EVENT_ENCRYPT_KEY`, `OPENFORGE_FEISHU_ALLOWED_CHAT_IDS`, and `OPENFORGE_FEISHU_USER_MAPPINGS_JSON`. It must call existing repository methods `upsertConfig`, `replaceUserMappings`, and `configurePublicWebhook`; it must emit JSON containing `ok`, `publicWebhookId`, `callbackPath`, `publicWebhookEnabled`, `integrationEnabled`, `identityMode`, `allowedChatIdCount`, and `mappingCount`. It must not print verification token, event encrypt key, raw encrypted secret JSON, JWT, or raw DB rows. Add root script `smoke:feishu-public-webhook` that runs the helper through `tsx`.</action>
  <verify>pnpm --dir packages/gateway test ../../scripts/prepare-feishu-public-webhook.test.ts</verify>
  <acceptance_criteria>
    - If no helper is added, the summary identifies the existing safe setup command and why it satisfies the same environment-variable and redaction requirements.
    - If added, `scripts/prepare-feishu-public-webhook.ts` exists and reads required webhook secrets from environment variables instead of command-line arguments.
    - If added, `scripts/prepare-feishu-public-webhook.test.ts` asserts successful output contains `callbackPath` and count fields but not the raw verification token or event encrypt key.
    - If added, `package.json` contains `smoke:feishu-public-webhook`.
    - The helper never logs raw `verificationToken`, `eventEncryptKey`, `verification_token_encrypted`, `event_encrypt_key_encrypted`, `X-Lark-Signature`, or `Authorization`.
  </acceptance_criteria>
  <done>Operators have a reproducible secret-safe way to prepare the current DB for real Feishu callback verification.</done>
</task>

<task type="manual">
  <name>Task 2: Run CLI and Gateway callback preflight</name>
  <files>docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md</files>
  <read_first>.planning/phases/OF-07-feishu-live-callback-readiness/07-CONTEXT.md, .planning/phases/OF-07-feishu-live-callback-readiness/07-RESEARCH.md, docs/API.md, packages/gateway/src/routes/integrations-feishu.ts, packages/gateway/src/server.ts</read_first>
  <action>Create `docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md` with rows for `CLI preflight`, `Gateway callback setup`, `Real console URL verification`, `Optional real message event`, `Local signed route regression`, `Authority regression`, and `Secret/redaction scan`. Run `lark-cli auth status --verify` and `lark-cli doctor`; record only status, CLI version, advisory update notice, and no raw auth/config content. Prepare Gateway callback config with the helper or the existing safe command. Record the callback URL shape as `https://<public-host>/api/v1/integrations/feishu/webhook/<publicWebhookId>` without exposing tokens or event encrypt key.</action>
  <verify>rg -n "CLI preflight|Gateway callback setup|Real console URL verification|Optional real message event|Local signed route regression|Authority regression|Secret/redaction scan|/api/v1/integrations/feishu/webhook/" docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md</verify>
  <acceptance_criteria>
    - `docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md` exists.
    - The report contains separate rows for `CLI preflight`, `Gateway callback setup`, `Real console URL verification`, `Optional real message event`, `Local signed route regression`, `Authority regression`, and `Secret/redaction scan`.
    - CLI evidence is labeled as preflight only and not as FEI-01 pass.
    - Gateway callback setup evidence includes `publicWebhookId`, callback path/URL shape, enabled state, allowed chat count, mapping count, and owner/rerun path.
    - The report does not contain raw Feishu auth config, raw verification token, event encrypt key, `X-Lark-Signature` value, JWT, or raw callback body.
  </acceptance_criteria>
  <done>Preflight and setup evidence is recorded without overclaiming callback success.</done>
</task>

<task type="manual">
  <name>Task 3: Attempt real developer-console callback verification</name>
  <files>docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md</files>
  <read_first>docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md, docs/API.md, packages/gateway/src/routes/integrations-feishu.ts, packages/gateway/test/feishu-integration.test.ts</read_first>
  <action>If a public HTTPS URL and Feishu developer-console access are available, configure Feishu event subscription callback URL to `https://<public-host>/api/v1/integrations/feishu/webhook/<publicWebhookId>` and run URL verification. Record `Real console URL verification` as `Pass` only if Feishu developer console successfully validates the callback. If no URL, console access, or callback setup is available, record `Caveat` or `Blocked` with exact missing prerequisite, owner, and rerun action. If a real message event can be triggered, subscribe to `im.message.receive_v1`, send a message in an allowed chat, and record `Optional real message event` as `Pass`, `Caveat`, or `Blocked`. Do not record raw request body, raw message text, tokens, signatures, or app secrets.</action>
  <verify>rg -n "Real console URL verification|Pass|Caveat|Blocked|owner|Rerun|Optional real message event" docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md</verify>
  <acceptance_criteria>
    - `Real console URL verification` status is not `Pending`.
    - FEI-01 `Pass` is used only if the report says a real Feishu developer-console HTTP callback occurred.
    - If `Real console URL verification` is `Caveat` or `Blocked`, the row includes missing prerequisite, owner, and rerun action.
    - `Optional real message event` is either `Pass`, `Caveat`, or `Blocked` and clearly states whether a real Feishu message event was attempted.
    - The report contains no raw Feishu app secret, verification token, event encrypt key, request body, signature value, JWT, or private chat text.
  </acceptance_criteria>
  <done>Real Feishu console callback status is truthfully recorded as pass, caveat, or blocker.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `git diff --check`
- [ ] `pnpm --dir packages/gateway test ../../scripts/prepare-feishu-public-webhook.test.ts` if a setup helper was added
- [ ] `rg -n "Real console URL verification|CLI preflight|Gateway callback setup|Optional real message event|Pass|Caveat|Blocked|Rerun" docs/reports/phase-7-feishu-callback-evidence-2026-05-21.md`
- [ ] Targeted scan over Phase 7 report and any helper tests for raw `sk-`, `Bearer `, `OPENFORGE_MASTER_KEY=`, `OPENFORGE_JWT_SECRET=`, `verificationToken:`, `eventEncryptKey:`, `X-Lark-Signature:`, `APP_SECRET`, `raw request body`, or private message text.
</verification>

<success_criteria>
- FEI-01 has a real callback `Pass`, or a precise `Caveat`/`Blocked` row with owner and rerun instructions.
- CLI long-connection/event-consumer evidence is not confused with real console callback proof.
- Callback setup evidence is reproducible without logging raw Feishu secrets.
- Plan 07-02 can consume the Feishu report and finalize exposure caveats plus regression evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/OF-07-feishu-live-callback-readiness/07-01-SUMMARY.md`.
</output>
