import { CopilotAccessPage } from "@/components/copilot/CopilotAccessPage";

/** Grant management page inside the Copilot settings center. */
export default function CopilotAccessRoute() {
  return <CopilotAccessPage />;
}
